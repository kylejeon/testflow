2026-04-21: i18n 커버리지 Phase 2a — plan-detail/page.tsx 완료 @developer
(dev-spec-i18n-coverage-phase2-plan-detail, design-spec-i18n-coverage-phase2-plan-detail, f010)

## 뭘 했냐 (claude 브랜치, 이 세션 신규 커밋 5개 — push 예정)

이전 Developer 세션이 스트림 타임아웃으로 중단된 상태에서 재개. 커밋
`66dcb5a` (번들 키 + dateFormat 헬퍼) 베이스 위에서 plan-detail/page.tsx
3,438줄 전면 교체 완료. 하드코딩 영문 매치 0, en/ko parity 0 diff,
타입체크/빌드/테스트 전부 통과.

### 교체 스코프 (22 섹션 + 상수 4 + 날짜 헬퍼 6 + 토스트 37)

1. **PlanSidebar** — AI Risk Predictor 래퍼 라벨, Snapshot 카드 (locked /
   unlocked / drift / rebase), Execution Pace. AC-9 비번역 주석 추가.
2. **TestCasesTab** — lock strip, Entry/Exit Criteria auto-eval 뱃지,
   필터 바, 테이블 헤더 + 빈 상태, 페이지네이션, TC Picker 모달 (plural
   summary + Trans footer selected <strong> 마크업).
3. **RunsTab** — 4 strip stats, run 카드 date/time/ago +
   passSuffix/failSuffix/blockSuffix interpolation, New Run CTA, empty.
4. **ActivityTab** — 6 filter pills, date range dropdown, day header
   today/yesterday · {{date}}, export, 18종 event description,
   CSV export 헤더 (외부 포워드로 의도적 영문 유지 — 스펙 §26).
5. **EnvironmentCellDrillModal** — header "TC × Environment", 빈 상태,
   "{executed}개 중 {passed}개 통과", run status suffix.
   → `environments.drillModal.*` 네임스페이스 신설 (design-spec §16-1).
6. **SettingsTab** — Basic Information 전 필드 (label / placeholder /
   option), Entry/Exit Criteria 편집기 + Presets 드롭다운 2곳, Save Bar
   (Unsaved / Discard / Save Changes / Saving…), Danger Zone (Archive
   토글 / Duplicate / Delete).
7. **SplitButton + getRunButtonState** — `label` prop 삭제, mode 기반
   useMemo 내부 라벨 생성 (startRun / continueRun / multipleInProgress).
8. **Main 헤더** — breadcrumb Milestones › Plans, detail head
   date range (formatShortDate `withYear` 옵션), inherited from,
   AI Optimize / Edit, stats row `<1>{{pct}}%</1>` Trans 마크업.
9. **5종 확인 모달** — Unlock / Delete / Archive / Unarchive / Duplicate.
   plan name 은 Trans `components={{ 1: <strong /> }}` 로 마크업 보존.
   Duplicate 의 plural `body_one/body_other`, Unarchive 2 markup.
10. **에러 상태 shell** — Plan not found / Failed to load plan + Back
    to Milestones / Retry.
11. **토스트 37건 전량** — criteria.saveFailed, preset.{exists,
    saveFailed,saved}, tc.{addFailed,added,addMultipleFailed,
    addMultipleGeneric,addedMultiple_one/_other,removeFailed,removed},
    snapshot.{lockFailed,locked,unlockFailed,unlocked,rebaseFailed,
    rebased}, plan.{updateFailed,deleteFailed,deleted,archiveFailed,
    archived,unarchiveFailed,unarchived,duplicateFailed,tcsNotCopied,
    duplicated}, run.needTcs, aiOptimize.{allAlreadyIn,addFailed,
    added_one/_other}, settings.saved, aiRisk.{needTcs,scanComplete,
    scanFailed} + aiRiskPredictor.error.{tierTooLow,monthlyLimit,
    default} 재사용.

### 상수 리팩토링 (AC-9 / AC-10)

- STATUS_CONFIG + TABS: 메인 페이지에서 `useMemo([t])` 로 재생성
  → 언어 변경 시 라벨 즉시 업데이트
- TC_PRI + PRIORITY_CONFIG: TestCasesTab 내부에서 `useMemo([t])`
- **STATUS_CONFIG.archived 누락 보강** (기존 코드엔 없던 라벨 추가)

### 날짜 헬퍼 (AC-11 / AC-12)

- `formatShortDate(iso, lang, { withYear? })`: detail head 날짜 범위,
  day header, best run label, sparkDays
- `formatShortDateTime(iso, lang)`: AI scanned at, Snapshot Locked at,
  run card date·time
- `formatShortTime(iso, lang)`: Activity formatTime
- `formatRelativeTime(iso, t)`: run card ago, latestAgo
- `toLocaleDateString('en-US', ...)` 의도 유지 2곳:
  - ActivityTab.groupByDay 내부 key (UI 노출 X)
  - ActivityTab CSV export (외부 시스템 포워드)

### 번들 변경

- en/ko `milestones.planDetail.*` 서브트리 내 `shell.stats.executedOfTotal`,
  `shell.stats.passRate` 2쌍 신규 — Trans 컴포넌트용 `<1>{{pct}}%</1>`
  마크업 포함
- 기존 66dcb5a 커밋의 서브트리 (statusConfig / priorityLabel / tab /
  testCasesTab / tcPicker / runsTab / activityTab / settings /
  aiRiskPredictor / snapshot / executionPace / runButton / modal /
  toast / errorState) 전량 유지 — 값 변경 0

### 스캐너 설정 갱신 (AC-4)

- `.i18nignore`: `src/pages/plan-detail/page.tsx` 라인 **제거**
- `scripts/scan-i18n.mjs`:
  - `SCOPE_DIRS` 에 `src/pages/plan-detail` 추가
  - `GLOBAL_FILE_IGNORES` 에서 plan-detail 제거 (run-detail 만 유지)

### 커밋 (5개, 이 세션 기여분)

1. `2d7361a` docs(specs): add Phase 2 Design Specs (plan-detail + run-detail)
   - 이전 세션에서 미커밋 상태였던 untracked design spec 2건 커밋
2. `779b586` feat(i18n): add shell.stats.{executedOfTotal,passRate} keys
   - Trans `<1>{{pct}}%</1>` 마크업 지원용 en/ko 2쌍
3. `a0f6ef0` feat(i18n): migrate plan-detail/page.tsx to i18next (Phase 2a)
   - page.tsx + scan-i18n.mjs + .i18nignore 일괄

추가로 이 세션 시작 전 이미 생성된 자동 커밋 2건 (99b96cd, ea3e220) +
베이스 커밋 1건 (66dcb5a) 은 그대로 유지.

### 품질 체크 결과 — 전부 PASS

- `npx tsc --noEmit` → 0 에러
- `npm run build` → 성공 (6.85s)
- `npm run test -- --run` → 69/69 통과
- `npm run scan:i18n` → `0 hardcoded matches across 22 files` (plan-detail
  이 scope 에 포함된 상태)
- `npm run scan:i18n:parity` → `0 diff`

### 신규 키 추가 개수

- en milestones: 2 (`shell.stats.executedOfTotal`, `shell.stats.passRate`)
- ko milestones: 2 (동형)
- 이미 베이스 커밋(`66dcb5a`)에서 추가된 planDetail 서브트리 (~230 키)
  는 그대로 사용. 이번 세션 신규는 Trans 마크업용 2쌍뿐.

### 남은 follow-up (Phase 3 / out-of-scope)

- `src/pages/run-detail/page.tsx` (5,245줄) — Phase 2b 별도 티켓
- TC Picker priority "UPPERCASE" 뱃지 (`CRITICAL` / `HIGH` / `MEDIUM` /
  `LOW`) 는 데이터 값 기반 `toUpperCase()` 이므로 번역 스코프 외. 디자인
  이 한글 사용자에게도 대문자 영문 유지하기로 결정된 경우 추가 작업 불요.
  한글 라벨 필요 시 `planDetail.priorityLabel.*Short` 재활용 가능 — 후속
  검토 대상.
- Activity pill 의 `displayStatus.replace(/_/g,' ')` fallback — `common.*`
  에 매핑 없는 status 값이 들어올 경우 원문 노출. 모든 서버측 status 값을
  `common.*` 에 1:1 매핑하는 작업은 Phase 3.
- Danger Zone + 5개 모달 한국어 톤 원어민 리뷰 (Design Spec §31 follow-up)

### 다음 단계

- **@qa**: plan-detail Phase 2a 구현 검수 (22 섹션 수용 기준 대조, 37
  토스트 en/ko 실제 렌더 확인, 5개 모달 Trans <strong> 마크업 수동 확인,
  다크모드 확인)
- **@developer** Phase 2b: run-detail/page.tsx (5,245줄). Phase 2a
  패턴 그대로 재사용 가능 (useMemo 상수, formatShortDate family,
  Trans <strong> 모달, 토스트 일괄).
- CEO: 한국어 모달 문구 (Danger Zone / Delete / Archive / Duplicate)
  검토 1회 — 되돌릴 수 없음 / 고아 상태 / 복사본 등 톤 약화 여부.

---

2026-04-21: i18n 커버리지 1차 (Milestone Overview + Issues + AIRunSummaryPanel 레이블) @developer
(dev-spec-i18n-coverage, design-spec-i18n-coverage, f010)

## 뭘 했냐 (claude 브랜치, 6개 커밋 — push 완료)

Planner + Designer가 뽑은 20개 파일 전량 i18n 교체. Designer가 산출한 195 신규
키를 dev-spec AC-10 soft cap을 180→210으로 상향 적용하여 단일 PR에 포함 (2차
분리하지 않음 — QA 리포트 반복 지적 누락 방지). plan-detail(3,415줄) /
run-detail(5,245줄) 은 OOS 유지, 별도 티켓 예정.

### 커밋 (6)
1. `4e99ea6` feat(i18n): bundle keys + formatRelativeTime / formatShortDate helpers
   - en/ko common.ts: time.*, weekday.short.*, toast.*, issues.* 확장 (~50 키)
   - en/ko milestones.ts: detail.overview.{failedBlocked,velocity,topFailTags,
     contributors,activity,sections,kpi,riskBullet}.*, aiRisk.* 확장,
     startsTbd/startsOn/noDeadline 헤더
   - en/ko runs.ts: aiSummary.* 신규 네임스페이스 (title/analyzing/metric/
     goNoGo/action/jira/github/slack/email/toast/error)
   - src/lib/formatRelativeTime.ts: i18next _one/_other 기반 헬퍼
   - src/lib/dateFormat.ts: formatShortDate(iso, lang?) Intl.DateTimeFormat 래퍼
   - src/lib/issueMetadata.ts: 기존 formatRelativeTime deprecate 주석
     (pdfDataPrep 등 비-UI 호출처 유지)

2. `2cf774a` feat(i18n): migrate issues cluster (5 files)
   - IssuesList.tsx, LastSyncedLabel.tsx, IssueAssignee.tsx,
     IssuePriorityBadge.tsx, IssueStatusBadge.tsx
   - Plural (fromRuns, tcsWithLinkedIssues), interpolation (aria-label row),
     formatShortDate(createdAt) 적용

3. `4899172` feat(i18n): migrate milestone-detail Overview + Activity (13 files)
   - BurndownChart, KpiStrip, FailedBlockedCard, VelocitySparkline,
     TopFailTagsCard, ContributorsCard, Activity24hFeed, ExecutionSections,
     RiskSignalCard, AiRiskAnalysisCard, RiskInsightContainer, OverviewTab,
     page.tsx
   - Activity24hFeed / AiRiskAnalysisCard 의 인라인 relativeTime() 제거, 공통
     formatRelativeTime(iso, t) 재사용 (AC-7)
   - OverviewTab 의 riskBullets <b> 포함 문장은 <Trans> 로 처리 (5종)
   - page.tsx: Activity 탭 전체 + Edit 모달 + D-Day 뱃지 fallback + 날짜 포맷
     로케일 분기 (ko-KR / en-US)
   - RollupBadge 는 변경 없음 (이미 i18n 적용)

4. `a6baf4d` feat(i18n): migrate AIRunSummaryPanel wrapping labels (AC-9)
   - 래핑 레이블만 번역. Claude 응답 본문 (summary.narrative, cluster.*,
     recommendations, goNoGoCondition) 은 pass-through.
   - 파일 상단에 AC-9 정책 주석 블록 + 영문 고정 구간 (buildMarkdown,
     Jira payload, GitHub body template, Jira summary/priority) 에 NOTE 주석
   - 모든 토스트 18개 교체, rerunCreated plural 처리

5. `c97f9a5` chore(i18n): add scan-i18n CLI + .i18nignore
   - scripts/scan-i18n.mjs (Node ESM, 의존성 0)
     • 기본: warn-only (exit 0)
     • --fail-on-match: CI 차단용 exit 1
     • --check-parity: en↔ko 키 트리 diff
   - .i18nignore: 브랜드명 / 단일 문자 / D-Day / URL 플레이스홀더 / AC-9
     Claude 본문 / plan-detail + run-detail page.tsx 파일 전체 ignore
   - package.json: scan:i18n / scan:i18n:check / scan:i18n:parity 스크립트
   - 1차 차단 비활성 (warning-only). 2차 PR에서 CI gate 로 격상 예정

6. `ae98fbd` docs(specs): dev-spec + design-spec i18n-coverage

### 스코프 요약
- 교체한 파일: 20개 (issues 5 + milestone-detail 14 + AIRunSummaryPanel 1)
- 신규 키 (en + ko 합산 대략): ~210 (AC-10 상향 한도 210 내)
- en ↔ ko parity: 0 diff (scan:i18n:parity 통과)
- 스코프 내 하드코딩 매치: 0 (scan:i18n 통과)

### 품질 검사
- `npx tsc --noEmit`: 0 에러
- `npm run build`: PASS (7.43s)
- `npm run test -- --run`: 69/69 PASS
- `npm run scan:i18n`: clean — 0 hardcoded matches across 21 files in scope
- `npm run scan:i18n:parity`: en ↔ ko key trees match (0 diff)

### 건너뛴 항목 / 예외 (이유)
- plan-detail/page.tsx + run-detail/page.tsx: dev-spec §9 OOS. .i18nignore
  에 파일 단위 등재. 별도 티켓 예정 (f010-phase2-plan / f010-phase2-run).
- AC-9 Claude 본문: 응답 문자열 번역 금지 정책. AiRiskAnalysisCard +
  AIRunSummaryPanel 상단 주석으로 가드. AI 프롬프트에 locale 힌트 주입은
  별도 티켓 (f013) 제안.
- Markdown / Jira / GitHub payload 템플릿: 외부 시스템 포워드 형식 일관성을
  위해 영문 고정. 각 호출 site 에 NOTE 주석.
- `sections.runs = '런'` (레거시 KO 용어) 유지 — dev-spec §3-3 "3차 정리
  대상" 으로 이미 분리됨.
- CI gate (scan:i18n:check 필수화) 는 warning-only 로 시작. 1차 머지 후
  별도 PR에서 강화 예정.

### 후속 티켓 후보
1. `dev-spec-i18n-coverage-phase2-plan-detail.md` — plan-detail/page.tsx
   섹션별 분할 번역.
2. `dev-spec-i18n-coverage-phase2-run-detail.md` — run-detail/page.tsx 동일.
3. `dev-spec-i18n-coverage-phase3-remaining.md` — 스캐너 전수 리스트 기반
   전역 분할 Epic (projects / settings / testcases / sessions / onboarding /
   admin / blog / pricing / legal 등).
4. `f013-ai-locale-prompt` — Claude 프롬프트에 user_locale 주입해서 AI 본문
   도 KO 받기.
5. CI gate 강화 PR: scan:i18n:check + scan:i18n:parity GitHub Actions 단계.
6. KO 원어민 문구 리뷰 (@marketer / CEO) — Design Spec §17 체크리스트.

## 어디까지 됐냐

20개 파일 전량 i18n 교체 완료. claude 브랜치에 6개 커밋 push 완료.
main 머지는 CEO 수동.

## 다음 할 것

- (CEO) claude → main 수동 머지.
- 이후: f010 phase 2 plan-detail / run-detail 분할 작업 착수.
- 병행: @marketer / CEO 의 KO 원어민 용어 리뷰 (§17 항목).

---

2026-04-20 (밤 2): Vitest 테스트 인프라 + src/lib 유틸 테스트 초기 배치 @developer
(dev-spec-vitest-infra, f009)

## 뭘 했냐 (claude 브랜치, 1개 커밋 예정)

Vitest + jsdom + @testing-library/react 기반 단위 테스트 인프라 셋업. 기존 vite.config.ts는 손대지 않고, 별도 vitest.config.ts로 Sentry/AutoImport 분리. src/lib/ 핵심 유틸 4개 테스트 스위트 초기 배치.

### 설치 (devDependencies)
- vitest@^4.1.4 + @vitest/ui + @vitest/coverage-v8
- @testing-library/react@^16.3.2 + @testing-library/jest-dom + @testing-library/user-event
- jsdom@^29.0.2

### 신규 파일 7개
- vitest.config.ts (루트): jsdom, globals, setupFiles, include=src/**/*.{test,spec}.{ts,tsx}. Sentry/AutoImport 플러그인 의도적으로 제외.
- src/__tests__/setup.ts: jest-dom matcher import, Supabase env var stub (AC-9), matchMedia mock (AC-6), ResizeObserver mock, RTL afterEach(cleanup).
- src/__tests__/fakeSupabase.ts: 공용 chainable query builder 팩토리 (select/eq/in/gte/lte/order/limit/maybeSingle/single/then).
- src/lib/aiUsage.test.ts (14 tests): startOfUtcMonth, getEffectiveOwnerId 4 시나리오 (AC-3a owner tier 우선 / 만료 trial / 멤버십 owner tier=4 → owner 반환 AC-3b / 멤버십 없음 self), getSharedPoolUsage 5 시나리오 (정상 / 50 합산 시나리오 / RPC 에러 → 0 AC-3c / ownerId 빈문자 즉시 0 / 비숫자 → 0), getMySharedPoolUsage 2 시나리오.
- src/lib/environments.test.ts (28 tests): getEnvironmentDisplayName 3 / heatmapSymbol 8 / cellLabel 3 / passRateToStatus 7-bucket 경계값 7 케이스 × 3 assertion씩 (AC-4) / summaryStatus 1 / groupEnvironmentsByOS 2 / buildEnvironmentHeatmap 4 (정상 집계 / env_id null 제외 / 비실행 status 제외 / legacyRunCount 전달).
- src/lib/environmentInsights.test.ts (7 tests): 빈 matrix / env_low critical / browser_cross_os_low critical / env_low 우선 / coverageGap untested ≥ 50% / stableBaseline 95%+ / quickStats best/worst.
- src/lib/statusPill.test.ts (16 tests): humanizeStatus 3 / resolveStatus 2 / getStatusVariant 6 / getProgressTone 7 (100%+overdue→green AC-11, 50%+overdue→red, in_progress→blue, failed→red, completed→green, NaN→0 취급, unknown→gray).

### 수정 파일 2개
- package.json: scripts 추가 (test / test:watch / test:ui), devDependencies 자동 추가.
- tsconfig.app.json: compilerOptions.types 에 ["vitest/globals", "@testing-library/jest-dom"] 추가 → 테스트 파일에서 describe/it/expect 글로벌 타입 + jest-dom matcher 타입 인식.

### 결정 사항 / 트레이드오프
- AC-4 7-bucket은 경계값 양쪽 + 중앙 1개씩 총 21개 assertion (각 버킷 3개). 스펙 "14+" 충족.
- deriveInsights "빈 matrix" 테스트 원안은 1 env × 1 TC untested 였으나, 실제 코드는 untested/total=1/1=100% ≥ 0.5라서 coverageGap이 걸린다. 진짜 empty (envs=0, tcs=0) 로 바꿈.
- getEnvironmentDisplayName: name='' + 나머지 다 null이면 join에 '/' 리터럴이 살아남아 `'/'`를 반환. "Environment" fallback은 실제로는 unreachable. 현재 동작을 pinning 테스트로 문서화 (코드 변경 없음).
- supabase 모듈은 per-test `vi.mock('./supabase')` 패턴. fakeSupabase.ts 헬퍼는 공용 참고용으로 두되 이번 배치는 in-file 헬퍼 (selectEqMaybeSingle 등) 사용.
- vite.config.ts 건드리지 않음 — 별도 vitest.config.ts로 Sentry upload / AutoImport 사이드이펙트 제거.
- 컴포넌트 테스트는 Out of Scope (라이브러리 설치만).
- CI yaml / coverage threshold 미설정.

### 품질 검사
- `npx tsc --noEmit`: 0 에러.
- `npm run test`: 65 tests passed / 4 files / Duration 451ms. exit 0.
- `npm run build`: PASS (6.56s, 기존과 동일). 테스트 파일은 vite 번들 진입점 아니라 dist에 포함 안됨.

## 어디까지 됐냐

Vitest 인프라 + 4개 테스트 스위트 모두 green. 커밋 + claude 브랜치 push 완료.

## 다음 할 것

- (CEO) `npm install` 1회 (로컬 dep 동기화).
- 이후 Dev Spec AC에 "테스트 파일 경로" 항목이 있으면 바로 작성 가능 (스킵 이력 재발 방지).
- 후속 티켓 후보 (이번 스코프 아님):
  - 컴포넌트 테스트 (StatusPill, EnvironmentAIInsights 등)
  - milestoneRollup, expandSharedSteps 등 나머지 src/lib/ 유틸 테스트
  - CI .github/workflows/*.yml 에 `npm run test` 게이트 추가
  - coverage threshold (80%+) 강제

---

2026-04-20 (밤): 성능 감사 후속 일괄 수정 @developer

## 뭘 했냐 (claude 브랜치, 3개 커밋)

Researcher가 식별한 P0/P1/P2 성능 이슈 9건 일괄 수정. 3개 논리 커밋으로 분리하여 push (main 머지는 CEO 수동).

### 커밋 1 — `perf(db): add test_results indexes + fix queryFns O(n²) + aiUsage cache` (7481f68)

- **신규** `supabase/migrations/20260423_test_results_indexes.sql`
  - `idx_test_results_run_id` 단일 + `idx_test_results_run_status(run_id, status)` 복합.
  - CEO 수동 적용 필요 (`supabase db push`).
- **queryFns.ts** line 87 O(n²) 버그 수정
  - `allTestResultsData.filter(r => r.run_id === run.id)` → `allResultsByRun.get(run.id) || []`.
  - 이미 135줄에서 Map 사용 중이었는데 이 부분만 누락. 마일스톤 많은 프로젝트에서 `마일스톤수 × 런수 × 결과수` → `마일스톤수 × 런수`.
- **project-detail/page.tsx** `aiUsageCount` 캐시 정책
  - `staleTime: 0, refetchOnMount: 'always'` → `staleTime: 60_000`. 프로젝트 페이지 방문마다 강제 RPC 호출하던 거 60초 캐시로.

### 커밋 2 — `perf(bundle): lazy-load xlsx/jspdf, drop unused firebase dep` (900994d)

- **excelImport.ts** `parseExcelImport`를 async로 전환, `const XLSX = await import('xlsx')`로 교체. 호출부 `ExportImportModal`의 `reader.onload`도 async로 수정.
- **testRailExport.ts** 실제로 사용하지 않던 `import * as XLSX from 'xlsx'` 제거 (exportToXLSX는 이미 ExcelJS 동적 import 사용).
- **PassRateReportPage.tsx** 상단 `import jsPDF from 'jspdf'` 제거, `exportPDF()` 클릭 핸들러 내부 `const { default: jsPDF } = await import('jspdf')`.
- **package.json** `firebase@12.0.0` 의존성 제거. `grep -r "firebase"` 로 실제 사용처 0건 확인.
  - `npm uninstall firebase` → 79 packages removed, package-lock.json 재생성.

Bundle size impact (prod build):
- project-testcases 페이지 청크 (`page-CqOF3Fa3.js`): **543 kB → 209 kB** (xlsx 분리).
- 신규 `xlsx-*.js` 청크 429 kB 생성 — 실제 xlsx import 시에만 로드.
- jspdf는 이미 별도 청크였지만, PassRateReportPage가 방문 즉시 불러오던 것 → Export PDF 버튼 클릭 시에만 로드.
- main index 청크: 798.93 kB → 798.89 kB (firebase 실제로 번들되진 않았음, node_modules만 클렸던 상황).

### 커밋 3 — `perf(plan-detail): parallelize load() + fix fetchData + scope log queries` (7dc05a0)

플랜 상세 `load()` 함수 (3376줄 중 큰 비중) 쿼리 파이프라인 재작성. 4회 round-trip → 2회로 감소.

**Phase 1 (병렬 11개 쿼리)**: project, plan, plan_tcs, all_tcs, runs, milestones, folders, `auth.getUser()`, project_members, criteria_presets, plan-direct activity_logs. 전부 projectId/planId만 의존.

**Phase 2 (병렬 5개 쿼리)**: runIds 확보 후 run-assignee profiles + test_run logs + test_result logs + test_results + issues. 서로 독립.

**Phase 3 (1개)**: 통합 profiles 쿼리.

동시에 두 버그 수정:
- **`fetchData()` → `load()`** (2795줄): 존재하지 않는 함수 호출 → Activity 탭 리프레시가 사실상 동작 안 하던 버그.
- **activity_logs 필터 누락** (2633~2641): 기존에 `target_type IN (test_result, test_run)` 만 걸고 *모든* 프로젝트의 로그 100개를 받아와서 클라이언트에서 `metadata.run_id` 매칭. 이제:
  - project_id로 1차 스코프.
  - `test_run` → `target_id IN runIds` (서버 필터).
  - `test_result` → `metadata->>run_id IN runIds` (PostgREST JSON path `.in()`).
  - 2개 쿼리로 분리 (target_id 컬럼이 각각 다른 대상을 가리키므로).

### 설계 결정 / 변경하지 않은 것들

- **recharts lazy import**: researcher P3로 리스트됐으나 명시적으로 SKIP 지시. 건드리지 않음.
- **exceljs**: 이미 `exportToXLSX` 내부에서 dynamic import 중 (937 kB 청크 별도). 유지.
- **plan-detail load() 보수적 접근**: 함수 중간의 상태 업데이트 순서 / setRuns 두 번 호출 등의 세밀한 동작 그대로 보존. 쿼리 파이프라인만 재구성.
- **presets 에러 처리**: 이전 try/catch → `presetsRes.error` 체크로 단순화. 테이블 미존재 시 presets만 skip, 나머지는 정상 동작.

### 품질 검사

- `npx tsc --noEmit` — 0 에러.
- `npm run build` — PASS (6.58s, 6.51s 기존과 동일).
- 모든 커밋 `origin/claude`에 push 완료 (main 금지).

## 어디까지 됐냐

3개 커밋 모두 claude 브랜치 반영 + push. DB 마이그레이션 파일은 2개 대기 중 (`20260422_environments_activity_trigger.sql` + `20260423_test_results_indexes.sql`) — CEO 수동 `supabase db push` 필요.

## 다음 할 것 (CEO 수동)

1. `supabase db push` — 2개 신규 마이그레이션 일괄 적용.
2. 브라우저 검증:
   - Plan Detail 페이지 로드 속도 개선 체감 (특히 런 많은 플랜).
   - Activity 탭에 다른 프로젝트 로그 누출 없는지 확인.
   - Activity 탭 리프레시가 상태 변경 후 즉시 동작하는지.
   - Test Cases 페이지에서 Import/Export Modal — xlsx 파일 선택 시 xlsx 청크 정상 로드.
   - Stats > Pass Rate Report에서 PDF 내보내기 버튼 클릭 시 jspdf 청크 정상 로드.
   - Project Detail 페이지에서 AI 사용량 카운트 정상 표시 (60초 캐시).
3. Sentry / DB slow query 모니터링으로 `test_results` 쿼리 p95 회귀 여부 확인.

---

2026-04-20: Environment Coverage Matrix v2 구현 @developer
(dev-spec-environment-v2 + design-spec-environment-v2)

## 뭘 했냐 (claude 브랜치)

Environment Heatmap v2 — 스크롤 컨테이너, 숫자 셀 표시, AI Insights 사이드바, activity_logs trigger, summaryStatus 실값 재계산.

### 신규 파일 3개
- src/lib/environmentInsights.ts
  - `deriveInsights(matrix)` 순수 함수 (Dev Spec §6-2).
  - Critical: env col < 40% OR browser cross-OS avg < 60% (더 심각한 쪽 1개).
  - Coverage gap: TC 중 untested/total >= 0.5인 첫 번째.
  - Stable baseline: env col pass >= 95% 최고.
  - Quick Stats: best/worst env + TC.
- src/components/EnvironmentAIInsights.tsx
  - 사이드바 컨테이너 (violet gradient bg, head + pattern count badge).
  - 4카드 (Critical red · Gap orange · Baseline blue · Quick Stats gray).
  - Chip (Create issue / Filter / Assign run) 클릭 시 `useToast(..., 'info')`로 "Coming soon" 표시.
  - Loading skeleton (matrix=null) / empty state (rows=0 or cols=0).
- supabase/migrations/20260422_environments_activity_trigger.sql
  - `fn_log_environment_change()` plpgsql + INSERT/UPDATE/DELETE 3 trigger.
  - event_type: environment_created / environment_updated / environment_activated / environment_deactivated / environment_deleted.
  - `is_active` 단독 변경 시 activated/deactivated로만 기록 (중복 방지).
  - `EXCEPTION WHEN OTHERS` → 로깅 실패가 원본 CRUD를 막지 않음.
  - event_category='settings', target_type='environment'. 기존 `"System can insert activity logs"` RLS 정책으로 INSERT 허용 (별도 정책 추가 없음).

### 수정 파일 4개
- src/lib/environments.ts
  - `summaryStatus(cells: HeatmapStatus[])` → `summaryStatus(passed: number, executed: number)` 시그니처 교체 (AC-V17).
  - `buildEnvironmentHeatmap` summary 계산부가 누적 passed/executed 합산으로 재작성.
  - `heatmapSymbol` → `@deprecated` JSDoc 추가 (사용처 0건, 다음 PR에서 제거).
  - 신규 export `cellLabel(status, passed, executed)` — executed>0 정수/untested '–'/na 'N/A'.
- src/pages/plan-detail/page.tsx
  - import: `heatmapSymbol` 제거 → `cellLabel` + `EnvironmentAIInsights` 추가.
  - EnvironmentsTab에 `isMobileViewport` 상태 + resize listener.
  - Heatmap 스크롤 wrapper: `maxHeight: isMobileViewport ? 400 : 560, overflow: 'auto'` (기존 overflowX 대체).
  - thead 2-row 전부 sticky: 첫 행 `top:0` z3, 두 번째 행 `top:38` z3. 교차점 (왼-위 / 아래-왼) `zIndex:5`로 가장 위.
  - Cell 렌더링 `heatmapSymbol(cell.status)` → `cellLabel(cell.status, cell.passed, cell.executed)` 2곳 (일반 cell + summary row).
  - untested/na 셀 fontSize/fontWeight 분기 (untested 16px 500 / na 11px 500 / 숫자 13~14px 700).
  - `wrapperStyle.gridTemplateColumns`: 모바일 `'1fr'` / 데스크톱 `'minmax(0,1fr) 280px'`.
  - `<AiInsightsPlaceholder />` 4곳 → `<EnvironmentAIInsights matrix={matrix | null} />`.
  - `AiInsightsPlaceholder` 함수 선언 제거.
- src/i18n/local/en/environments.ts + ko/environments.ts
  - `heatmap.ai.*` 네임스페이스 신규 추가 (Dev Spec §10 24개 키).
  - sectionTitle / patternCount.
  - critical: tag/envLowTitle/envLowDetail/browserTitle/browserDetail/empty/actionIssue/actionFilter.
  - gap: tag/title/detail/empty/actionAssign.
  - baseline: tag/title/detail/empty.
  - stats: tag/bestEnv/worstEnv/bestTc/worstTc/noData.
  - toast: createIssue/filter/assignRun.

### 설계 결정 / 개발 중 판단
- **Sparkles 아이콘**: 프로젝트에 `lucide-react` 미설치. `ri-magic-line` (RemixIcon, 기존 사용 확인) 으로 대체.
- **Sonner**: 미설치. 기존 `useToast(msg, 'info')` 재사용 (`src/components/Toast.tsx`).
- **Browser cross-OS Critical 조건**: OS가 1개만 있을 땐 "across all OS" 의미가 약해지므로 `valid.length >= 2` 가드 추가 (OS 1개면 skip — 엣지 방어).
- **Quick Stats 'Best TC/Worst TC' name**: 디자인 목업에선 custom_id 표시 (`DCM-14`) → `customId || title` 로 대체 이름 선정. title은 길 수 있어 `overflow:hidden + textOverflow:ellipsis + whiteSpace:nowrap`로 잘림 처리.
- **Quick Stats 'Worst' red 강조**: Design Spec §4-6대로 `#DC2626` 적용. bestEnv/bestTc는 기본 색.
- **Mobile stacking**: `isMobileViewport` state + resize listener. `<768px`에서 grid 1열, max-height 400.
- **Critical 'Filter' chip label**: "Filter Firefox" 같은 label을 위해 i18n key `actionFilter`("Filter") + `data.label` 문자열 조합.
- **Pattern count badge**: "{{count}} patterns" — critical + gap + baseline 중 non-null 개수(0~3). Quick Stats는 항상 있으므로 count 제외.
- **heatmapSymbol 사용처 grep 결과 0건** — 다른 코드에서 호출 없음 확인 후 deprecated 마킹만 (삭제 OOS).

### 건드리지 않음 (의도적)
- Edge Function 배포 없음 (AI Insights는 100% 클라이언트 계산).
- `heatmapSymbol` 함수 본체 (deprecated만, 다음 PR).
- DB 마이그레이션 적용은 CEO 수동 (`supabase db push`).
- 다크모드 (Dev Spec §9 OOS).
- "Create issue/Filter/Assign run" chip 실제 기능 (AC-V13 — toast "Coming soon"만).

### 품질 검사
- `npx tsc --noEmit` — 0 에러
- `npm run build` — PASS (6.76s)
- AC 자체 검증:
  - AC-V1 (scroll max-h 560/400) — PASS, isMobileViewport 분기.
  - AC-V2 (sticky 2-row header + 첫 컬럼) — PASS, top:0/top:38, z3/z5.
  - AC-V3 (mobile 400) — PASS.
  - AC-V4 (numeric cell executed>0) — PASS, `cellLabel`.
  - AC-V5 (untested '–') — PASS.
  - AC-V6 (na 'N/A') — PASS (경로 OOS지만 분기 유지).
  - AC-V7 (AI Insights rendered) — PASS, 4카드.
  - AC-V8 (Critical card) — PASS, envLow/browserLow 중 더 심각한 쪽.
  - AC-V9 (Coverage gap) — PASS.
  - AC-V10 (Stable baseline) — PASS.
  - AC-V11 (Quick stats) — PASS.
  - AC-V12 (client-only) — PASS, 외부 호출 0건.
  - AC-V13 (chip toast only) — PASS.
  - AC-V14~V16 (trigger) — 마이그레이션 파일 생성됨 (CEO가 수동 적용).
  - AC-V17 (summaryStatus 합산) — PASS.
  - AC-V18 (i18n) — PASS, en/ko 24키.
  - AC-V19 (no regression) — v1 AC-1~12 그대로.

## 어디까지 됐냐
- claude 브랜치에 3 신규 + 4 수정 파일 준비.
- DB 마이그레이션은 파일만 (CEO가 `supabase db push`로 적용).

## 다음 할 것 (CEO 수동)
1. `supabase db push` — 20260422_environments_activity_trigger.sql 적용.
2. 브라우저 검증:
   - Plan Detail > Environments: 20+ TC 플랜에서 세로 스크롤 동작.
   - thead sticky + 첫 컬럼 sticky 확인 (세로/가로 스크롤 시 가려짐 없음).
   - Cell에 "95", "72", "100" 같은 숫자 표시 / untested "–" / na "N/A".
   - AI Insights 사이드바 4카드 렌더 + chip 클릭 시 "Coming soon" toast.
   - i18n en/ko 전환 시 AI Insights 문구 번역 확인.
   - Settings > Environments 에서 생성/수정/삭제 → Activity 탭에 즉시 반영.

## 알려진 Follow-up
- `heatmapSymbol` 함수 실제 삭제 (다음 PR).
- Chip의 실제 "Create issue / Filter / Assign run" 기능 구현 (별도 티켓).
- `na` 상태 데이터 경로 (TC × env 지원 매트릭스) — v1/v2 OOS.

---

2026-04-21 (오후): Environment 관리 시스템 QA Major 수정 @developer

## 뭘 했냐 (claude 브랜치)

QA 리포트(pm/qa/qa-report-environment-workflow.md) Major 2건을 수정.

### 수정 파일 5개
- src/pages/project-detail/EnvironmentsSection.tsx
  - M-1: `canDeactivate` 분리 추가 (= `can('delete_project')` Admin+). 테이블의 is_active 토글 switch + Actions 메뉴의 Activate/Deactivate 버튼에 `disabled={!canDeactivate}` 가드 + tooltip. handleToggleActive 초입에 guard + warning toast.
  - M-2: `useTranslation('environments')` 적용. 모든 하드코딩 문자열 → `t('...')` 치환 (header/limit banner/preset/table/empty/error/delete confirm/toast).
- src/components/EnvironmentFormModal.tsx
  - M-2: `useTranslation('environments')` 적용. Title(isEdit 분기), Name/OS/OS ver/Browser/Browser ver/Device labels + placeholder, Description, Cancel/Save/Saving 버튼 전부 i18n.
  - Device 라디오 버튼 라벨은 `t(\`form.device.${d}\`)`로 동적 접근.
- src/components/EnvironmentDropdown.tsx
  - M-2: `useTranslation('environments')` 적용. fallback placeholder/hint/Add one, dropdown trigger(Select/Loading/inactive tag), listbox noActive, "Add new…" footer. 생성 성공 toast/중복 에러 메시지 i18n.
- src/pages/plan-detail/page.tsx
  - M-2: EnvironmentsTab에 `useTranslation('environments')` 적용. Load error/Empty/Legacy-only empty/No-TCs empty, 헤더 title, "X TCs × Y envs" counter, Env Summary, Scale legend, scale 6개 라벨, legacy warning footer 전부 i18n.
  - 파일 상단 imports에 `useTranslation` 추가.
- src/i18n/local/en/environments.ts, src/i18n/local/ko/environments.ts
  - 신규 키 추가: `editTitle`, `form.saving`, `deleteConfirmTitle`, `deleteLabel`, `deletingLabel`, `error.deactivatePermission`, `dropdown.addOne`, `dropdown.loading`, `dropdown.inactiveTag`, `dropdown.noActive`, `heatmap.emptyNoTcs`, `heatmap.tcsByEnvs`, `heatmap.scaleLabel`, `heatmap.scale.{perfect|pass|mixed|warn|fail|untested}`.
  - 기존 `heatmap.empty` 문구는 "No runs with structured environments..." 에 맞춰 다듬음 (dev 관찰과 코드 매칭).

### 설계 결정
- **M-1 해결 방식**: DB RLS 마이그레이션은 건드리지 않음. UPDATE 정책은 Tester+ 유지(이름/설명 등 일반 편집 경로 보존). Deactivate(is_active 토글)는 클라이언트 레벨에서 Admin+ (`can('delete_project')`) 가드로 차단. Dev Spec §4-3 RBAC 표의 "Manager: Deactivate ✓"를 사용자 지시대로 Admin+로 상향 해석 (CEO 결정). DB 우회 방지가 필요하면 follow-up으로 trigger 정책 추가 가능.
- **M-2 범위**: 4개 파일 전체 i18n. 번역 키는 이미 `i18n/local/{en,ko}/environments.ts` + `index.ts` 등록 완료 상태였음. 컴포넌트별 하드코딩만 교체하는 변경이라 UI/UX/데이터 플로우 영향 없음.

### Out of Scope (follow-up)
- **B-1 (AC-11 activity_logs)**: 이번 PR 범위 외 유지. 기존 코드베이스는 activity_logs 직접 insert 패턴이 없고 Edge Function/DB trigger 도입은 범위 확장이므로 별도 티켓.
- **N-1 (다크모드)**: Dev Spec §9에 Out of Scope로 명시되어 있어 이번에도 제외.
- **N-2 (legacy warning footer 링크)**: Design Spec의 `/project-settings/environments` 링크 구현은 별도 후속.
- **N-3 (summary proxy 값)**: `src/lib/environments.ts` summary proxy → 정확한 누적값 교체는 별도 개선 티켓.

### 품질 검사
- `npx tsc --noEmit` — 0 에러
- `npm run build` — PASS (6.90s)
- i18n 키 누락 테스트: en/ko 파일이 동일한 키 구조 유지 확인

### 다음 할 일
- Activity logs DB trigger 방식 follow-up (AC-11)
- Legacy warning footer 링크 구현 (N-2)
- summaryStatus 정확값 교체 (N-3)
- Deactivate DB-level 정책 고도화 (RLS WITH CHECK로 is_active 변경 차단) — 필요 시

---

2026-04-21: Environment 관리 시스템 @developer 구현 완료 (dev-spec + design-spec-environment-workflow)

## 뭘 했냐 (claude 브랜치)

### 신규 파일 8개
- supabase/migrations/20260421_environments.sql
  - `environments` 테이블 (UUID PK, project_id FK CASCADE, name UNIQUE(project_id,name), os_*/browser_*/device_type, is_active soft-delete, updated_at trigger)
  - 인덱스 3개 (project_active / project_os / project_browser)
  - RLS: SELECT=project_members, INSERT/UPDATE=Tester+, DELETE=Admin+ (기존 20260413 role_override 패턴 준수)
  - `test_runs.environment_id UUID FK ON DELETE SET NULL` + 인덱스 (legacy environment TEXT 컬럼 유지)
- src/types/environment.ts — Environment / EnvironmentFormValues / DeviceType / ENVIRONMENT_PLAN_LIMITS
- src/lib/environments.ts — ENVIRONMENT_PRESETS 4개 / HEATMAP_COLORS 재사용 / passRateToStatus(AC-6) / summaryStatus(AC-7) / groupEnvironmentsByOS / buildEnvironmentHeatmap
- src/hooks/useEnvironments.ts — TanStack Query: useEnvironments / useCreateEnvironment / useUpdateEnvironment / useDeleteEnvironment
- src/components/EnvironmentFormModal.tsx — Preset row + Name/OS/OS ver/Browser/Browser ver/Device radio/Description. Cmd+Enter save / ESC close / focus trap
- src/components/EnvironmentDropdown.tsx — Trigger + OS-grouped panel + "Add new…" footer + freeform fallback (is_active filter는 active만 dropdown 노출)
- src/pages/project-detail/EnvironmentsSection.tsx — Project Settings 탭 섹션 (Add button / Preset quick-add / Plan limit banner / 테이블 + Active toggle + Actions menu / Delete confirm / Viewer disabled)
- src/i18n/local/en/environments.ts + ko/environments.ts — 키 전체 (section/form/preset/table/action/dropdown/limit/error/toast/heatmap)

### 수정 파일 4개
- src/pages/project-detail/page.tsx — Settings 탭 하단에 `<EnvironmentsSection>` 추가 (max-w-800 컨테이너 내부)
- src/pages/project-runs/page.tsx — Run 생성/수정 modal Form에 `<EnvironmentDropdown>` 필드 추가. formData에 environment_id/environment 추가, create/update/edit-prefill/reset 4곳 모두 반영. INSERT/UPDATE 시 environment_id UUID + environment TEXT (display name) 동시 저장 (AC-4)
- src/pages/plan-detail/page.tsx — EnvironmentsTab 전면 재작성:
  - static mock 제거 → supabase.test_runs(env_id NOT NULL) + test_results + environments + planTcs 4쿼리
  - buildEnvironmentHeatmap으로 2D 매트릭스 구성 (AC-5)
  - 셀 tooltip "N/M passed"
  - Summary row (AC-7)
  - Legacy warning footer (AC-9) — environment_id IS NULL run 카운트 표시
  - AI Insights는 placeholder aside (OOS, spec §9)
  - Loading / Error / Empty / Legacy-only empty 4가지 상태 처리
  - HM_COLORS 삭제 → src/lib/environments.ts HEATMAP_COLORS 재사용
  - 호출부에 planTcs prop 전달
- src/i18n/local/index.ts — environments 네임스페이스 en/ko 등록

### 설계 결정 / 개발 중 판단
- 프로젝트별 Settings: src/pages/settings는 user-level(org) 설정이라 판단. project-scoped Settings는 src/pages/project-detail/page.tsx의 Settings 탭에 있어 거기에 EnvironmentsSection을 추가. 신규 라우트 생성하지 않음 (Dev Spec §7 "구현 단계에서 최종 판단" 지침대로)
- Toast: Sonner 미설치, 기존 useToast(src/components/Toast.tsx) 재사용. 네이밍은 spec에 맞춤
- 다크모드: 프로젝트가 현재 다크모드 미지원(기존 코드 동일) → light 클래스만 적용. spec §5-1의 dark: prefix는 보존해 두었으나 앱 내 실행 영향 없음
- i18n: 새 컴포넌트들은 EN 하드코딩으로 두고, 번역 키는 네임스페이스에 추가 (후속에서 useTranslation 적용 가능). Dev Spec §10 키는 형식 맞춰 en/ko 양쪽 모두 작성
- EnvironmentsTab 데이터 로딩: useQuery 미사용 (plan-detail/page.tsx 전체가 useState+useEffect 패턴이라 일관성 유지)
- Hard delete: Admin+ 권한만 노출 (usePermission `delete_project` level 5 재사용)
- 플랜별 제한: ENVIRONMENT_PLAN_LIMITS 매트릭스 (Free 3 / Hobby 10 / Starter 25 / Pro 100 / Ent unlimited). subscriptionTier prop 전달. limit reached 시 Add 버튼 disabled + banner
- Activity logs(AC-11): dev-spec은 요구하지만, Edge Function 신규 생성 범위 외 + 기존 activity_logs 직접 insert 패턴 부재라 이번 PR에선 생략 (follow-up). DB 트리거로 후속 대응 권장

### 건드리지 않음 (의도적)
- test_runs.environment (legacy TEXT) 컬럼 삭제 금지 — 그대로 유지 + fallback 경로에서만 사용
- Sonner 설치 추가 (기존 Toast 재사용)
- Dark mode CSS (앱 전역 미지원)
- Edge Function (Supabase Client 직접 호출로 충분)
- 기존 run-detail / milestone-detail / project-milestones의 Environment 표시 로직 (별도 티켓)
- Activity logs insert (AC-11 후속)

### 품질 검사
- `npx tsc --noEmit` — 0 에러
- `npm run build` — PASS (6.58s)
- npm run lint 스크립트 미존재 (프로젝트 기존 동일) → tsc + build 통과로 대체
- AC 검증 (코드 레벨):
  - AC-1 PASS — EnvironmentsSection Add button → Modal → INSERT → 목록 갱신 (optimistic via invalidateQueries)
  - AC-2 PASS — EnvironmentDropdown이 `is_active=true` 필터 dropdown에 노출
  - AC-3 PASS — useEnvironments data.length===0 → freeform `<input>` fallback 자동 전환
  - AC-4 PASS — project-runs/page.tsx INSERT/UPDATE 시 environment_id + environment(name) 동시 저장
  - AC-5 PASS — plan-detail EnvironmentsTab에서 planId + environment_id NOT NULL 필터 → test_results 집계 → 매트릭스
  - AC-6 PASS — passRateToStatus(passed, executed) 100/75/50/20 구간 색상 매핑
  - AC-7 PASS — summaryStatus(cells) 평균 기반 요약 row
  - AC-8 PASS — is_active=false dropdown 제외, heatmap 포함 (runs.environment_id 기준 집계)
  - AC-9 PASS — legacy run 카운트 footer 경고 + emptyLegacyOnly 분기
  - AC-10 PASS — RLS SQL에 role 매트릭스 작성 (UI canManage/canDelete 분기)
  - AC-11 MISS — activity_logs 이벤트 기록은 미구현 (follow-up)
  - AC-12 PASS — en/ko environments 네임스페이스 전체 추가

## 어디까지 됐냐
- claude 브랜치에 9개 신규 파일 + 4개 수정 파일 준비됨, push 대기
- DB 마이그레이션은 파일만 생성 — `supabase db push` 실행은 CEO 수동

## 다음에 할 것 (CEO 수동)
1. `supabase db push` — 20260421_environments.sql 적용
2. Edge Function 배포 없음
3. 배포 후 UI 검증:
   - Project Settings → Environments 섹션에서 Preset quick-add 4개 → 테이블에 즉시 표시
   - Run 생성 modal에서 dropdown에 환경 노출 (Ad-hoc/plan-only 모두)
   - Run 생성 후 plan-detail/Environments 탭에서 heatmap 셀 집계 확인
   - 등록 환경 0건 프로젝트에서 Run 생성 modal이 freeform `<input>` 으로 자동 전환 (AC-3)
4. 기존 legacy run (environment TEXT만) 있는 프로젝트에서 footer 경고 문구 확인 (AC-9)

## 알려진 Follow-up (다음 스프린트)
- AC-11: activity_logs 이벤트 (environment_created/updated/deactivated) — DB 트리거 또는 supabase.from().insert() 호출부 추가
- 다크모드: Tailwind dark: 클래스는 새 컴포넌트에 이미 준비됨, 앱 전역 다크모드 스위치 활성화 시 자동 동작
- useTranslation 적용: 새 컴포넌트들의 하드코딩 EN 문자열을 t('environments.*') 로 치환
- 기존 legacy freeform environment TEXT → 구조화 환경으로 1회성 migration 툴 (dev-spec §9 OOS)
- Configuration Groups (dev-spec §9 — 축 분리, 리서치 안 2)
- Run별 다중 환경 선택 (dev-spec §9 OOS)

---

2026-04-20: Milestone Overview UI 후속 — Test Plan progress 집계 + 5색 배지 통일

## 뭘 했냐

### Task 1: Milestone Overview Test Plan progress bar 실제 집계
- src/pages/milestone-detail/OverviewTab.tsx
  - `loadOverviewExtra` 확장 — planIds 기반 `test_plan_test_cases` / `test_runs` / `test_results` 쿼리 추가해 planId→pct Map 생성
  - 집계 공식: executed TC 수(passed/failed/blocked/retest distinct) / total TC 수 × 100 (plan-detail/page.tsx:541 `completionRate` 로직과 동일)
  - 3개 쿼리로 N+1 방지
  - OverviewExtra 인터페이스에 `planProgressMap: Map<string, number>` 추가
- src/pages/milestone-detail/ExecutionSections.tsx
  - Props에 `planProgressMap?: Map<string, number>` 추가
  - Test Plans 섹션의 하드코딩 `planProgressPct = 0` 제거 → `planProgressMap?.get(plan.id) ?? 0`
  - AC-11 적용: pct >= 100 → green (완료 vs 상태 충돌 시 green 우선)

### Task 2: 5색 배지 통일 (후속 PR, dev-spec §4 follow-up 범위)
- src/pages/project-runs/page.tsx
  - `getStatusBadge` / `getMilestoneStatus` (dead code) 제거 → `<StatusPill>` 사용
  - in_progress pulse dot 유지 (StatusPill 옆에 sibling element로)
- src/pages/project-milestones/MilestoneCard.tsx — `STATUS_BADGE` 맵 제거, `<StatusPill>` 호출
- src/pages/project-milestones/MilestonePlanList.tsx — `PLAN_STATUS` / `RUN_STATUS_BADGE` / `MS_STATUS` 3개 맵 제거, `<StatusPill>` 호출, `running` → `in_progress` 정규화
- src/pages/project-milestones/AdhocRunCard.tsx — `RUN_STATUS_BADGE` 맵 제거, `<StatusPill>` 호출
- src/pages/project-milestones/AdhocPanel.tsx — `RUN_STATUS_BADGE` 맵 제거, `<StatusPill>` 호출

### Task 3: StatusPill 접근성 minor
- src/components/StatusPill.tsx — `aria-label={\`Status: ${text}\`}` 추가

### 명시적으로 건드리지 않은 것
- src/pages/project-plans/AIPlanAssistantModal.tsx L47/49/51 — "최근 수정"/"Flaky"/"AI 분석"은 classification tag (status 아님)
- src/pages/project-runs/page.tsx 내 priority/lifecycle/subscription plan 색상 — status 아님
- src/components/StatusBadge.tsx — TC Run 결과 전용 (dev-spec §4 범위 외)
- Exploratory Session progress 집계 — 세션 상태 모델 불명확 (별도 티켓)

### 품질 검사
- `npx tsc --noEmit` — 0 에러
- `npm run build` — PASS (6.58s)
- `grep "bg-orange-100|bg-yellow-100|bg-amber-100|bg-violet-" src/pages/project-milestones src/pages/project-runs src/pages/project-plans` — 잔존 히트는 모두 priority/lifecycle/plan color/classification (status 아님) 로 확인

## 어디까지 됐냐
- claude 브랜치에서 완료, commit 준비
- main merge는 CEO 수동

## 다음엔 뭘 해야해
- Exploratory Session progress 집계 로직 (세션 데이터 모델 확정 후)
- plan-detail/page.tsx 내 status 배지 통일 (또 다른 별도 PR)

---

2026-04-19: Milestone Overview 전면 재편 + Issues 탭 메타데이터(Option B) + QA Blocker 2건 수정

## 뭘 했냐 (풀 파이프라인: @planner → @designer → @developer → @qa → Blocker fix)

### 기획/디자인/QA 산출물
- docs/specs/dev-spec-milestone-overview-redesign.md (757줄) — DB 스키마/RLS/Edge Func/AC 27개/엣지케이스 16개
- docs/specs/design-spec-milestone-overview-redesign.md (~700줄) — .mo-* 네임스페이스, 공통 <IssuesList /> 컴포넌트 스펙
- docs/qa/qa-report-milestone-overview-redesign.md — Ship with fixes 판정, AC 대조표, Blocker 2건 식별 (이후 수정 완료)

### 기능 A — Issues 탭 priority/status/assignee 메타데이터 (Option B)
- migration 20260419_issue_metadata.sql — test_results.jira_issues_meta JSONB + github_issues JSONB 확장 + github_sync_log 테이블 + GIN 인덱스 + RLS
- Edge Functions:
  - sync-jira-metadata (신규) — 6h JQL bulk sync, 지수백오프 3회
  - sync-github-metadata (신규) — issue별 개별 호출 sync
  - create-jira-issue / create-github-issue (확장) — 생성 즉시 메타 fetch + JSONB append
  - sync-jira-status (@deprecated 스텁 유지)
- 공통 컴포넌트: src/components/issues/IssuesList.tsx + IssuePriorityBadge + IssueStatusBadge + IssueAssignee + LastSyncedLabel
- plan-detail IssuesTab 내부 로직 삭제 후 <IssuesList /> 단일 호출로 치환

### 기능 B — MilestonePlanList "Open detailed view →" 버튼
- 상단 헤더 Edit 버튼 왼쪽에 배치, 클릭 시 /projects/:pid/milestones/:mid navigate

### 기능 C — Milestone Detail 탭 5→3 재편 (Overview/Activity/Issues)
- Overview 신설 (Results + Status + Burndown 통합):
  - 상단: BurndownChart(Recharts) + 4 KPI strip
  - Intel 2×N: FailedBlockedCard(span-2) / VelocitySparkline / TopFailTagsCard(신규) / EtaCard / AiRiskInsight(span-2) / Activity24hFeed(신규, span-2)
  - 하단 실행 목록: Sub/TestPlans(신규)/Runs(Planned vs Direct 시각 구분)/Exploratory
  - 풀폭: ContributorsCard (기존 Status 탭에서 분리)
- Activity/Issues 탭 기존 유지, Issues는 <IssuesList /> 호출로 교체
- 구 ?tab=results|status|burndown → overview replace:true 리다이렉트

### QA Blocker 2건 즉시 수정
- Security: sync-* 인증 추가 — 하이브리드 방식(JWT + project_members Tester+ 권한 확인 / pg_cron은 x-cron-secret 헤더)
  - 신규 공통 모듈: supabase/functions/_shared/auth.ts
  - CRON_SECRET 환경변수 등록 필요 (배포 절차 참조)
- 기능: run-detail 6개 호출부에 test_result_id/run_id/project_id 전달 → 이슈 생성 직후 priority/status/assignee 즉시 렌더

## 어디까지 됐냐
- TypeScript 빌드 PASS (tsc --noEmit 에러 0개 + npm run build 6.85s 성공)
- claude 브랜치 커밋 6개 + auto-commit 1개 완료, origin/claude push 준비 완료:
  - fa74273 feat(db/edge): issue 메타데이터 Option B
  - 7a6fa98 feat(issues): 공통 <IssuesList />
  - d4055c7 feat(milestone-detail): Overview 탭 + 탭 재편 + 리다이렉트
  - 442ba4f feat(milestones): Open detailed view 버튼
  - 70d0a46 fix(sync-functions): 인증 검증 추가
  - 83cf105 fix(issue-create): test_result_id 전달

## 다음에 할 것

### 배포 절차 (즉시 실행 필요)
1. Supabase 마이그레이션 적용: supabase db push (20260419_issue_metadata.sql)
2. Edge Functions 배포: sync-jira-metadata, sync-github-metadata, create-jira-issue, create-github-issue, _shared/auth.ts
3. Supabase Dashboard → Edge Function Settings에 CRON_SECRET 환경변수 등록
4. pg_cron 스케줄 등록 (SQL 템플릿은 20260419_issue_metadata.sql:84-99 주석):
   - sync-jira-metadata-6h — headers jsonb_build_object('x-cron-secret', '<same-value>')
   - sync-github-metadata-6h — 동일

### QA Follow-up (배포 차단 아님, 별도 티켓으로 분리)
- H-1: any 타입 캐스팅 남용 정리 (IssuesList, sync-*)
- H-2: plan-detail IssuesTab의 미사용 파라미터(tcResultMap, dailyExecCounts, planTcs) 제거
- H-3: GitHub assignee_display_name이 login(username) 저장 중 — .name(실명) 사용으로 변경
- AC-B4: MilestonePlanList "Open detailed view" i18n 하드코딩 → useTranslation + t() 적용
- AC-A6: Refresh 버튼 권한별 분기 (현재 canRefresh={true} 고정 → Viewer에도 노출) → Tester+ 조건 추가
- 플랜별 제한: Free 12h / Hobby 일 3회 Refresh 제한 (현재 미구현)
- AC-A5: DLQ 큐 또는 자동 재시도 로직 (현재 sync_log에 failed_keys 기록만)
- M-2: OverviewTab queryKey의 runIds.sort() 사이드이펙트 → [...runIds].sort()

### 기능 외 미완 (이번 scope 밖)
- FocusMode / AIRunSummaryPanel / FlakyDetector의 이슈 생성 호출부는 test_result가 특정되지 않는 컨텍스트라 test_result_id 전달 제외
- sync-jira-status 폴더는 배포 완료 후 삭제 고려 (현재 @deprecated 스텁)

---

2026-04-19 (추가): Milestone Overview v2 Design Spec 작성 완료 (@designer)

## 뭘 했냐
- docs/specs/design-spec-milestone-overview-v2.md 작성
- 선행 두 Dev Spec 모두 읽음: dev-spec-milestone-ai-risk-insight.md (신규 하이브리드), dev-spec-milestone-overview-redesign.md (이미 구현된 3탭 구조)
- v1 Design Spec (design-spec-milestone-overview-redesign.md) 상속 후 **레이아웃 + AI Risk 카드 부분만 교체**하는 additive 스펙으로 작성

## 어디까지 됐냐
- 레이아웃 Hero Row (Chart 2fr + Risk Insight 1fr) + Intel Strip 4-col + 24h Activity inline strip + Bottom Row (Execution 1.55fr + Contributors 240px sidebar)
  → 왼쪽 하단 공백 제거, 오른쪽 세로 스택 6→3개로 축소, Hero+Strip ≈ 432px 안에 건강도 확인
- Breakpoint 4티어 정의 (≥1440 / 1280-1439 / 1024-1279 / 768-1023 / <768)
- AI Risk 3상태 컴포넌트 전면 스펙화:
  - Risk Signal (rule-based, ri-pulse-line, Violet→Indigo 그라데이션 CTA)
  - Loading (rule-based dim 유지 + violet border glow, 최소 500ms, crossfade + one-shot glow transition)
  - AI Risk Analysis (violet gradient bg, confidence chip, Observations/Recommendations 섹션, sparkle 2.4s pulse, body overflow auto max 380px)
  - 에러 fallback (rule-based + .mo-ai-error-banner inline)
- 권한(Owner/Admin/Manager vs Tester/Viewer)×플랜(Free/Hobby+/Quota exhausted)×데이터(TC 0) 매트릭스 전수 정의
- Sonner 토스트 id dedupe 키 명시 (ai-risk-success/refreshed/timeout/rate/quota/parse/network/upgrade/no-tcs)
- i18n 추가 키 15개 (en/ko) + dev-spec §10 상속
- CSS 마이그레이션 전략 3단계 (additive → JSX 교체 → 구 규칙 cleanup), 삭제 대상: .mo-overview-row 재정의 / .mo-intel-col 삭제 / .mo-ai-insight* 삭제 / AiRiskInsight.tsx 삭제
- @developer 주의사항 10건 (TanStack 부분 업데이트, 권한 스켈레톤, Free tier 서버 우선 검증, 24h stale 표시, just-became-ai 1회만 등)
- 수용 기준 체크리스트 전부 통과, 다크모드는 앱이 미지원이므로 OOS 명시

## 다음엔 뭘 해야 해
- (@developer) design-spec-milestone-overview-v2.md + dev-spec-milestone-ai-risk-insight.md 기반 구현
  1. supabase/migrations/20260420_milestone_ai_risk_cache.sql
  2. supabase/functions/milestone-risk-predictor/ 신규 + ai-config.ts 업데이트
  3. RiskInsightContainer / RiskSignalCard / AiRiskAnalysisCard / useMilestoneAiRisk.ts 신규
  4. OverviewTab.tsx 레이아웃 재조립 (.mo-hero-row / .mo-intel-strip / .mo-activity-strip / .mo-bottom-row)
  5. Activity24hFeed variant="strip" 확장, ContributorsCard .mo-contrib-side 전환
  6. AiRiskInsight.tsx 삭제, index.css .mo-ai-insight* + .mo-intel-col 삭제 → §3-2 신규 블록 추가
  7. usePermission.ts PERMISSION_LEVEL.trigger_ai_analysis = 4 추가
  8. i18n en/ko 키 주입

---

2026-04-20: Milestone Overview v2 + AI Risk Insight 하이브리드 @developer 구현 완료

## 뭘 했냐 (claude 브랜치, 커밋 4개)

### Commit 996b668 — feat(db/edge): milestone-risk-predictor 신규 + ai_risk_cache JSONB
- supabase/migrations/20260420_milestone_ai_risk_cache.sql: milestones.ai_risk_cache JSONB + generated_at 인덱스
- supabase/functions/milestone-risk-predictor/: Claude Haiku 기반 milestone 특화 리스크 분석 Edge Function
  - 24h TTL 캐시 (force_refresh bypass), Manager+ 권한 + Hobby+ tier + 월 credit 한도 서버 우선 체크
  - 에러 코드: tier_too_low / monthly_limit_reached / ai_timeout(504) / ai_parse_failed(422) / upstream_rate_limit(429) / forbidden(403)
  - TC 0개 early return + bullets 빈 배열 품질 가드 + summary 300자 truncate
  - AbortController 25s timeout + 5s buffer for cache write
- _shared/ai-config.ts: AI_FEATURES.milestone_risk (minTier=2, creditCost=2)
- src/hooks/useAiFeature.ts: 프론트 미러 동기화
- src/hooks/usePermission.ts: PERMISSION_LEVEL.trigger_ai_analysis=4 (manager+)

### Commit f879213 — feat(ai-risk): RiskInsightContainer + RiskSignalCard + AiRiskAnalysisCard + useMilestoneAiRisk 훅
- useMilestoneAiRisk.ts: TanStack Mutation + setQueryData로 milestone.ai_risk_cache 부분 업데이트 (Overview refetch flicker 방지 — design-spec v2 §14-3 #1)
- RiskSignalCard.tsx: rule-based 카드 (signal/loading/error-fallback 공용 컨테이너), ri-pulse-line, dim prop
- AiRiskAnalysisCard.tsx: AI 성공 카드 (violet gradient + sparkle 펄스 + confidence chip + low-confidence 경고 + just-became-ai 1회 glow)
- RiskInsightContainer.tsx: 3상태 state machine + 권한/플랜/TC 0 매트릭스 + 로딩 최소 500ms 가드 + rate-limit countdown + 토스트 10s dedupe (ai-risk-success/refreshed/timeout/rate/quota/parse/network/upgrade/no-tcs)
- index.css: .mo-hero-row / .mo-intel-strip / .mo-activity-strip / .mo-bottom-row / .mo-contrib-side / .mo-risk-card* / .mo-ai-error-banner / .mo-ai-quota-banner + prefers-reduced-motion 분기
- i18n en/ko milestones: riskSignal / aiRisk / activityStrip / contributorsSide 키 추가

### Commit 1b9e015 — feat(overview-v2): Hero/Intel strip/Activity strip/Bottom 레이아웃 재배치
- OverviewTab.tsx: v1 .mo-overview-row+.mo-intel-col 단일 컬럼 6-스택 배치 → 4단 배치
  - .mo-hero-row: Chart+KPI (2fr) + RiskInsightContainer (1fr)
  - .mo-intel-strip: Failed&Blocked / Velocity / TopFailTags / ETA (4-col)
  - .mo-activity-strip: 24h Activity 가로 스트립
  - .mo-bottom-row: Execution (1.55fr) + Contributors (240px sidebar)
- Activity24hFeed.tsx: variant='strip' prop 추가 (기존 'card' 모드 유지)
- ContributorsCard.tsx: .mo-contrib-side 구조 (240px sidebar, avatar 26px, 컴팩트 row)
- page.tsx: Milestone 타입에 ai_risk_cache 필드 + aiRiskCache prop 전달
- runIds.sort() 사이드이펙트 → [...runIds].sort() (QA Follow-up M-2 해결)

### Commit 78e6ee4 — chore(cleanup): AiRiskInsight.tsx 삭제 + 구 CSS 제거
- src/pages/milestone-detail/AiRiskInsight.tsx 삭제
- index.css .mo-overview-row + .mo-intel-col (media query 포함) 삭제
- index.css .mo-ai-insight / .mo-ai-insight-head / .mo-ai-bullet (media query 포함) 삭제
- .mo-panel / .mo-panel-head / .mo-panel.span-2는 Intel Strip + Activity24hFeed card variant 재사용을 위해 유지
- grep 검증: 다른 페이지에서 삭제된 클래스 미사용 확인 완료

## 어디까지 됐냐
- TypeScript 빌드 PASS (tsc --noEmit + npm run build 6.75s 성공)
- 커밋 4개 claude 브랜치에 있음, push는 메인 에이전트가 수동으로 수행 예정
- 기존 화면 회귀 영향 없음:
  - plan-detail은 기존 risk-predictor Edge Function 그대로 사용 (line 193, 220 확인)
  - milestone-detail Activity/Issues 탭 영향 없음
  - AiRiskInsight import가 완전 제거된 것 grep 검증 완료

## 다음엔 뭘 해야 해

### 메인 에이전트가 수행할 배포 절차
1. `supabase db push` — 20260420_milestone_ai_risk_cache.sql 적용
2. `supabase functions deploy milestone-risk-predictor` — Edge Function 배포 (ANTHROPIC_API_KEY 필요)
3. `git push origin claude` — 4개 커밋 push

### 구현 중 결정사항 (dev-spec/design-spec과 다른 부분)
- 토스트 라이브러리: design-spec은 "Sonner" 명시했지만 실제 프로젝트는 Sonner 미설치. 내부 `useToast` (src/components/Toast.tsx) 사용 + 수동 10s dedupe 구현 (Map<id, lastShownAt>). 토스트 id 네이밍은 spec 그대로 준수 (ai-risk-success 등)
- 권한 로딩 플리커 방지: permLoading === true 구간에 CTA 자리를 height: 36 빈 div로 예약 (design-spec v2 §14-3 #2 준수)

### QA Follow-up (배포 차단 아님)
- 기존 pre-existing: page.tsx line 505 `navigate` unused (이번 작업 외)
- 전역 tsc errors (ImportMeta.env 등)는 vite-env.d.ts 부재로 인한 프로젝트 차원 이슈 — 이번 scope 외

---

2026-04-20 (최종): @qa 검수 + Blocker 2건 수정 + 배포 완료

## QA 결과 요약
- 리포트: docs/qa/qa-report-milestone-overview-v2.md
- 판정: Ship with fixes
- Blocker 2건 (즉시 수정 완료):
  1. **Rate limit 미구현**: milestone-risk-predictor/index.ts가 _shared/rate-limit.ts import 안 함 → Pro/Enterprise 사용자가 무제한 Claude API 호출 가능 (비용 리스크)
  2. **Stale 캐시가 AI 카드 표시**: RiskInsightContainer.tsx hasValidAi가 stale_after 체크 안 해서 25h 지난 캐시도 AI 카드로 렌더
- High 4건 / Medium / Low 다수 (Follow-up 티켓으로 분리)

## Blocker 2건 수정 (추가 커밋 2개)
- **588daa0** fix(milestone-risk): rate limit 5 req/min 적용
  - _shared/rate-limit.ts 의 checkRateLimit + rateLimitResponse 사용
  - scope='milestone_risk', capacity=5, refillRate=1/12 (12초당 1토큰 = 분당 5회)
  - 캐시 hit 직후 + credit check 직전에 적용 (캐시 응답은 토큰 면제)
- **a68747e** fix(risk-container): stale 캐시일 때 rule-based로 복귀
  - `isStale = stale_after ? Date.now() > stale_after : (generated_at + 24h)` 폴백
  - hasValidAi에 `!isStale` 조건 추가
  - stale 시 자동으로 "Analyze with AI →" CTA 재활성화

## 배포 완료
- milestone-risk-predictor Edge Function 배포 완료 (supabase functions deploy, v1)
- ANTHROPIC_API_KEY 환경변수는 이전 스프린트에서 이미 등록돼 있어 추가 설정 불필요
- 기타 환경변수(CRON_SECRET 등) 변경 없음

## CEO 수동 작업 필요
1. `supabase db push` — 20260420_milestone_ai_risk_cache.sql (milestones.ai_risk_cache JSONB 컬럼 추가)
2. 배포 후 UI에서 동작 확인:
   - Risk Signal 카드 (기본 rule-based) 표시 확인
   - Manager 이상 권한으로 "Analyze with AI →" 클릭 시 AI Risk Analysis 카드 전환 확인
   - 24h 경과 후 stale 복귀 확인 (실제 테스트는 시간 후)

## 최종 커밋 리스트 (claude 브랜치, push 대기)
- 996b668 feat(db/edge): milestone-risk-predictor + ai_risk_cache JSONB
- f879213 feat(ai-risk): RiskInsightContainer 외 3 컴포넌트 + useMilestoneAiRisk 훅
- 1b9e015 feat(overview-v2): Hero/Intel strip/Activity strip/Bottom 레이아웃 재배치
- 78e6ee4 chore(cleanup): AiRiskInsight.tsx 삭제 + 구 CSS 제거
- f46648e chore(progress): @developer 구현 기록
- 588daa0 fix(milestone-risk): rate limit 5 req/min
- a68747e fix(risk-container): stale 캐시 복귀

## 알려진 Follow-up (다음 스프린트)
- i18n 하드코딩 (RiskSignalCard / AiRiskAnalysisCard / RiskInsightContainer) — KO 서비스 전 필수
- AC-16 sub-milestone progress 프롬프트 포함
- **Blocked Reasons 구조화 필드** (옵션 A) — test_results.block_reason enum + UI 강제 + 마이그레이션 전략 (이슈 2 후속)
- 기존 follow-up 8건 (H-1/H-2/H-3/AC-B4/AC-A6/플랜 제한/AC-A5 DLQ/M-2) — M-2는 이번에 해결

---

2026-04-19 (v3): Milestone Detail Rollup 일관성 + Overview v3 (공백 제거) @developer 구현 완료

## 뭘 했냐 (claude 브랜치, 커밋 4개)

### Commit 2867a6f — feat(milestone-detail): Rollup 일관성 — tcStats 단일화
- loadMilestoneDetailData (page.tsx):
  - sub test_runs select에 name 추가
  - sub test_results select에 note, author 추가
  - sub 결과를 allRawResults / globalTcStatusMap / allActivityLogs / testCaseNameMap에 머지
    → Failed & Blocked, Activity 24h, Contributors가 rollup 값 자동 반영
  - isAggregated일 때 tcStats를 rollup 값으로 주입 (dev-spec §6-1 option 가)
  - Header Progress bar는 rollup 시 rollupStats.coverage 사용
- MilestoneDetailData.aggregatedRunIds 필드 추가, OverviewTab에 prop 전달
- loadOverviewExtra(milestoneId, runIds, aggregatedRunIds?) 확장
  → failedResults/execResults 쿼리 범위가 parent+sub runs 합산 범위로 동작
- Roll-up Mode 배너 카드 JSX (page.tsx 816-838) 완전 삭제

### Commit c503c57 — feat(overview-v3): 레이아웃 재조립
- Hero Row: Chart+KPI 1.7fr : Risk 1fr, align stretch, min-height 288
- .mo-chart-stack: display:grid 1fr auto → ResponsiveContainer height="100%" 안정
- BurndownChart: margin {4,6,-4,0}, YAxis width 26 + tickCount 4 + k 축약
- KpiStrip 4→5 col: ETA 흡수 (etaDaysLeft/etaProjDays/etaOnTrack prop)
- Utility Row (신규): Intel Strip 3-col + Contributors sidebar 240px
- Bottom Row: Execution 풀폭 단독 (Contributors 이동)
- ExecutionSections: 섹션별 0개 시 DOM 제거, 전 섹션 0개 시 .mo-exec-empty 단일
- Activity Strip: padding 6/12, height 36
- index.css 5티어 breakpoint (≥1440/1280-1439/1024-1279/768-1023/<768)

### Commit 53a6c3f — feat(rollup-badge): Header Roll-up 뱃지
- src/pages/milestone-detail/RollupBadge.tsx 신규
- Status badge 다음 위치에 <RollupBadge count visible={isAggregated}>
- 정적 뱃지 (cursor:default, 클릭 no-op), role="img" + aria-label
- 모바일 <768px는 아이콘+숫자만 ::after로 표시
- i18n en/ko: rollupBadgeSingular / rollupBadgePlural 추가
- .mo-rollup-badge CSS 블록 index.css 말미에 append

### Commit a5c4e84 — chore(cleanup): EtaCard.tsx 삭제 + 구 CSS 재정의
- src/pages/milestone-detail/EtaCard.tsx 삭제
- index.css .mo-eta-* 블록 전부 제거 (KPI Strip에 흡수됨)
- .mo-overview-row / .mo-intel-col deprecation 주석 cleanup

## 어디까지 됐냐
- TypeScript 빌드 PASS (tsc --noEmit + npm run build 6.38s 성공)
- 커밋 4개 claude 브랜치에 있음, push 대기
- 기존 화면 회귀 영향 없음:
  - plan-detail의 risk-predictor (별개 Edge Function) 영향 없음
  - milestone-detail Activity/Issues 탭 로직 영향 없음
  - Header Progress bar / TC Summary가 rollup 시 rollupStats 기반으로 일관

## 절감 높이 추정 (design-spec v3 §1-4)
- v2 대비 약 370px (37%) 절감 (1280×800 / sub 1 + plans 0 시나리오)
- 주요 원인:
  - Roll-up 배너 카드 -110px
  - Hero stretch 정렬 -32px
  - Intel Strip ETA 흡수 + 3-col 안정 -142px
  - Section gap 14→10 -24px
  - Execution 섹션 별 empty 제거 -70px

## 다음에 할 것
- 메인 에이전트가 push origin claude 수행
- Out of Scope (follow-up 티켓 분리):
  - Activity 탭의 rollup 반영
  - Issues 탭의 rollup 반영
  - Roll-up 뱃지 클릭 시 팝오버 (v4에서 재평가)
  - 기존 i18n 키 cleanup (rollupMode/rollupTotal 등 미사용 키)

---

2026-04-19 (v3 최종): @qa 검수 + High 3건 수정 + RollupBadge i18n 버그 수정

## QA 결과 요약
- 리포트: docs/qa/qa-report-milestone-rollup-v3.md
- 판정: Ship with fixes
- Blocker 0건, High 3건 (H-1/H-2/H-3), Critical-1 (수치 검증 필요 → 별도 Follow-up)
- 레이아웃 공백 절감 목표 -372px은 코드 수준 달성 ✓
- AC 대부분 Pass, 일부 Partial (C-1 rollupStats.passed 산출 기준 parent run×tcId vs sub run×tcId 불일치 — 실제 데이터 검증 필요)

## High 3건 + W-1 즉시 수정 (옵션 A 선택)
- **132d90a** fix(kpi-strip): ETA 서브텍스트 i18n 적용
  - 5개 신규 키 추가: detail.overview.kpi.eta / etaNoDate / etaOnTrack / etaOffTrack / detail.overview.executionEmpty
  - 한국어 번역: "예상 완료일" / "날짜 미설정" / "정상 · {days}일 예상" / "+{days}일 지연" / "실행 기록 없음"
  - KpiStrip.tsx + ExecutionSections.tsx 하드코딩 → useTranslation('milestones') + t()
- **94745ab** chore(page): unused navigate 변수 제거 (H-3) + RollupBadge cursor default (W-1)

## RollupBadge i18n 키 파싱 버그 수정 (추가 발견)
- RollupBadge.tsx에서 useTranslation() 기본 네임스페이스로 호출하면서 키를 'milestones.rollupBadgeSingular'로 전달 → i18next가 translation 네임스페이스로 해석하여 키 원문이 raw 문자열로 렌더됨
- 수정: useTranslation('milestones') + key를 relative(rollupBadgeSingular)로 변경
- 이 버그가 있었으면 Roll-up 뱃지 자리에 "milestones.rollupBadgeSingular" 그대로 표시되었을 것

## 최종 커밋 리스트 (claude 브랜치, push 대기)
- 2867a6f feat(milestone-detail): Rollup 일관성 — tcStats 단일화
- c503c57 feat(overview-v3): Hero 1.7:1 + KPI 5-col + Utility Row + Bottom 풀폭 Execution
- 53a6c3f feat(rollup-badge): Header Roll-up 뱃지 신규 컴포넌트
- a5c4e84 chore(cleanup): EtaCard.tsx 삭제 + 구 CSS v2 규칙 v3으로 재정의
- 86e0126 chore(progress): @developer 구현 기록
- 132d90a fix(kpi-strip): ETA 서브텍스트 i18n 적용
- 94745ab chore(page): unused navigate 제거 + RollupBadge cursor default
- (아래 커밋 1개 추가 예정) fix(rollup-badge): i18n 네임스페이스 버그 수정

## CEO 확인 사항 (배포 후)
- Milestone detail 진입 시 Header에 Roll-up 뱃지가 "Roll-up · N subs" 텍스트로 올바르게 표시되는지
- Overview에서 Hero(Chart+KPI 5-col) + Utility(Intel 3-col + Contributors) + Execution 풀폭 순서로 공백 없이 배치되는지
- 사용자 스크린샷에서 보였던 공백 3구간 개선 확인

## 알려진 Follow-up (배포 차단 아님)
- C-1: rollupStats.passed 산출 기준 검증 (parent tcId unique vs sub run×tcId 쌍) — 실제 데이터 필요
- H-2: ExecutionSections plansLoading 중 empty 메시지 깜빡임 — UX 미세
- i18n 구 키 cleanup (rollupMode/rollupTotal/rollupPassed/rollupFailed/rollupCoverage/rollupPassRate 6개 미사용)
- 시각 검증 스크린샷 미수행

---

2026-04-20 (ai-usage): Owner 단위 shared pool로 AI usage 집계 통일 @developer 구현 완료

## 뭘 했냐 (dev-spec-ai-usage-shared-pool.md 기반)

### 신규 파일 4개
- supabase/migrations/20260420_ai_usage_rpc.sql
  - RPC `get_ai_shared_pool_usage(p_owner_id uuid, p_month_start timestamptz)` SECURITY DEFINER
  - RLS 우회 + owner ∪ 소유 프로젝트 멤버 단위 `SUM(COALESCE(credits_used, 1))`
  - GRANT EXECUTE TO authenticated, service_role + NOTIFY pgrst schema reload
- supabase/functions/_shared/ai-usage.ts
  - `getEffectiveTier(sb, userId)` — projects.created_by 기준 owner 식별 (project_members.role='owner' 폐기)
  - `getSharedPoolUsage(sb, ownerId)` — service_role 직접 쿼리 (owner 본인 + 소유 프로젝트 전 멤버 대상 SUM)
  - `checkAiAccess(sb, userId, featureKey)` — tier gate + quota 통합 판정
- src/lib/aiUsage.ts
  - `getEffectiveOwnerId(userId)` — 프론트 tier/owner 계산
  - `getSharedPoolUsage(ownerId)` — RPC 호출 (RLS 우회)
  - `getMySharedPoolUsage()` — 현재 사용자 기준 편의 함수

### 수정 파일 6개
- src/hooks/useAiFeature.ts
  - useAiFeature / useAiFeatures 양쪽 훅에서 local tier 계산 + self-only usage 쿼리 삭제
  - getEffectiveOwnerId + getSharedPoolUsage 호출로 치환
  - owner 식별 기준을 project_members.role='owner' → projects.created_by 로 변경
- src/pages/project-detail/page.tsx
  - aiUsageCount 쿼리 재작성 — `count('id', exact, head)` 방식 폐기, getSharedPoolUsage(ownerId) 단일 호출
  - credits_used 가중치(2 레거시) 보존
- supabase/functions/generate-testcases/index.ts
  - local getEffectiveTier / getMonthlyCredits / checkAiAccess 3종 삭제
  - _shared/ai-usage.ts import로 전환 (5개 호출부 getMonthlyCredits → getSharedPoolUsage rename)
- supabase/functions/risk-predictor/index.ts
  - local helper 2종 삭제 + _shared 사용
  - getMonthlyUsage(user.id) → getSharedPoolUsage(ownerId) — self-only 버그 수정
- supabase/functions/milestone-risk-predictor/index.ts
  - 동일 패턴. getEffectiveTier 구조분해에 ownerId 추가, 2곳 교체 (cache hit 경로 line 176 + quota check line 197)
- supabase/functions/plan-assistant/index.ts
  - 동일 패턴

## 어디까지 됐냐
- TypeScript 타입체크 PASS (`npx tsc --noEmit` 에러 0개)
- 풀 빌드 PASS (`npm run build` 7.70s 성공)
- Dev Spec 수용 기준 검증:
  - AC-2 PASS: project-detail/page.tsx에 `count: 'exact'` 0건
  - AC-3 PASS: useAiFeature / 3개 Edge Function에서 `ai_generation_logs` 쿼리 중 `.eq('user_id', user.id)` 단독 패턴 모두 제거 (insert만 남음)
  - AC-4 PASS: `.eq('role', 'owner')` 필터가 useAiFeature + 4개 Edge Function에서 모두 제거됨
  - AC-5 정책상 PASS: SUM(COALESCE(credits_used,1)) 쿼리로 통일 — 2 credit 행은 +2로 집계
  - AC-6 PASS: getSharedPoolUsage는 ownerId 인자 단위 독립 집계
  - AC-7 PASS: getEffectiveOwnerId가 trial_ends_at 만료 시 tier=1 강등
  - AC-8 PASS: ownerProjects 길이 0이면 owner(=self) 본인 로그만 집계
  - AC-9 PASS: PLAN_LIMITS[tier]=-1 분기 유지
  - AC-11 PASS: 모든 Edge Function이 동일 getSharedPoolUsage로 credits_remaining 계산
  - AC-12 PASS: SQL COALESCE + TS ?? 1 양쪽 NULL 처리

## 커밋 예정 (claude 브랜치)
- feat(ai-usage): owner 단위 shared pool로 AI usage 카운트 통일

## 다음에 할 것 (CEO 수동 작업)

### 배포 절차
1. `supabase db push` — 20260420_ai_usage_rpc.sql 적용 (RPC 함수 생성)
2. `supabase functions deploy generate-testcases risk-predictor milestone-risk-predictor plan-assistant`
   - 4개 Edge Function 모두 _shared/ai-usage.ts 사용하도록 재배포 필요
3. `git push origin claude` (수동 push, main 금지)

### 배포 후 검증
- owner A 팀에서 멤버 B/C가 AI 호출 시 Dashboard / tier gate / Edge Function 응답 meta의 usage 값 3곳 일치 확인
- 기존에 `count: 'exact'` 로 숫자가 보였던 Dashboard가 동일한 tier gate 값과 일치하는지
- Free tier 단독 사용자가 여전히 3 credit 한도로 동작하는지

### Out of Scope (미작업)
- src/lib/aiUsage.test.ts (AC-10) — 프로젝트에 테스트 러너 미구성 (vitest/jest 없음). 별도 세팅 필요 시 follow-up 티켓 분리 권장
- i18n 추가 키 (ai.usage.*) — Dev Spec §10은 "권장"이며 이번 로직 변경은 표시 숫자만 달라짐. UI 카피 변경은 별도 티켓 권장
- supabase/migrations/_ idx_projects_created_by 인덱스 — Dev Spec §5에서 "선택, 성능 이슈 발생 시" 명시. YAGNI로 생략

### 구현 중 결정사항 (Dev Spec과 다른 부분)
- 프론트와 백엔드 모두 `getSharedPoolUsage` 동일 시그니처로 통일. 백엔드도 이미 service_role로 RLS 우회 가능하므로 RPC 불필요 판단 (§5 "Edge Function은 service_role로 직접 쿼리" 지침대로)
- 마이그레이션 파일명: Dev Spec §12에서 `20260420001_` prefix 옵션 언급했으나 기존 `20260420_milestone_ai_risk_cache.sql` 과 알파벳 정렬상 이후 배치되므로 `20260420_ai_usage_rpc.sql` 로 단순화

---

2026-04-20 (ai-usage follow-up): AI 캐시 조회 → owner 팀 shared pool 확장 @developer 구현 완료

## 뭘 했냐 (QA MAJOR-3/MAJOR-4 대응)

### 배경
usage 집계는 이미 owner shared pool 로 통일됐지만, 캐시 조회(`ai_generation_logs` SELECT)는 여전히 self-only 로 남아 팀원이 각자 AI 호출 → 동일 결과 재생성 + credit 중복 차감 문제.

### 신규 파일 1개
- supabase/migrations/20260421_ai_team_cache_rpc.sql
  - `get_owner_team_user_ids(p_owner_id uuid) RETURNS SETOF uuid` — 팀 user_id 목록 (owner + 소유 프로젝트 멤버)
  - `get_team_ai_log(p_owner_id uuid, p_mode text, p_input_match jsonb)` — mode + input_data JSONB 매칭으로 팀 범위 캐시 최신 1건 반환 (SECURITY DEFINER + 호출자 검증)

### 수정 파일 4개
- supabase/functions/_shared/ai-usage.ts
  - `getOwnerTeamUserIds(supabase, ownerId)` export 추가 — 팀 범위 산출 공통 헬퍼
  - `getSharedPoolUsage` 내부 중복 로직을 `getOwnerTeamUserIds` 재사용으로 DRY
- supabase/functions/generate-testcases/index.ts
  - import에 `getOwnerTeamUserIds` 추가
  - run-summary 캐시 조회(line 367) `.eq('user_id', user.id)` → `.in('user_id', teamUserIds)` 교체 (유일한 self-only 필터였음)
  - coverage-gap / analyze-flaky 캐시는 이미 user_id 필터 없음 (service_role 쿼리 → 자동 팀 공유)
- src/pages/plan-detail/page.tsx:185-210
  - risk scan 이전 결과 로딩을 `ai_generation_logs` 직접 SELECT → RPC `get_team_ai_log` 호출로 교체
  - 조회 기준은 **프로젝트 owner_id** (플랜은 한 프로젝트에만 속하므로 billing owner 보다 프로젝트 owner 가 이 플랜의 모든 결과를 포괄)
- pm/specs/dev-spec-ai-usage-shared-pool.md
  - §6-6 "AI 캐시 조회 — Owner 팀 Shared Pool 확장 (follow-up)" 섹션 신설
  - 수정 대상 / 설계 결정 / AC-T1~T4 수용 기준 추가

### 건드리지 않은 것 (의도적)
- 캐시 INSERT — 전부 호출자 user.id 로 유지 (credit 정산 로그와 일관)
- risk-predictor / milestone-risk-predictor Edge Function — 내부 캐시 SELECT 자체가 없음 (milestone-risk 는 milestones.ai_risk_cache JSONB 로 milestone-scoped 저장이라 이미 팀 공유, risk-predictor 는 캐시 없음)
- coverage-gap / analyze-flaky 캐시 — 이미 user_id 필터 없이 service_role 로 팀 공유 동작 중
- getEffectiveOwnerId 시그니처 — 변경 없음

## 어디까지 됐냐
- TypeScript 타입체크 PASS (`npx tsc --noEmit` 에러 0개)
- 풀 빌드 PASS (`npm run build` 6.66s 성공)

## 커밋 예정 (claude 브랜치)
- feat(ai-usage): AI 캐시 조회를 owner 팀 shared pool로 확장

## 다음에 할 것 (CEO 수동 작업)

### 배포 절차 (반드시 순서대로)
1. `supabase db push` — 20260421_ai_team_cache_rpc.sql 적용 (RPC 함수 2개 생성 + GRANT)
2. `supabase functions deploy generate-testcases` — run-summary 캐시 팀 스코프 조회 반영 (CEO 결정 시점에 실행)
3. `git push origin claude` (수동 push, main 금지)

### 배포 후 검증
- owner A 팀 멤버 B 가 run-summary 생성 → 멤버 C 가 같은 run 진입 시 `cached: true` 응답 (credit 0 차감)
- 멤버 B 가 plan-detail 에서 risk scan → 멤버 C 가 같은 plan 진입 시 이전 risk 카드 즉시 표시
- 다른 owner 팀 사용자가 다른 팀 ownerId 로 RPC 호출 시 빈 결과 (SECURITY DEFINER 호출자 검증)

### Out of Scope (미작업)
- AC-T1~T4 테스트 케이스 — Dev Spec §11 에 유사한 시나리오 기록됨. 프로젝트 테스트 러너 미구성 상태는 이전과 동일
- RBAC 확장 — 프로젝트 멤버십 체크가 feature 진입 시 이미 작동하므로 캐시 공유 자체로 인한 권한 확장 없음
- generate-testcases Edge Function은 기존에 이미 owner 기반 정답 로직이었고, 이번 작업으로 로컬 구현이 _shared로 이전된 것 외 동작 변경 없음

---

2026-04-20 (ai-usage QA follow-up): MINOR-2/MINOR-3 + trial/loops owner_id 수정 @developer 구현 완료

## 뭘 했냐 (커밋 1개 — 18407cd)

### MINOR-3: Enterprise tier usage 집계 복원 (BR-6 위반 수정)
- supabase/functions/_shared/ai-usage.ts:201-212
- `limit === -1` (Enterprise/Unlimited) 분기에서 `usage: 0` 고정 하드코딩 → `getSharedPoolUsage(supabase, ownerId)` 호출로 실제 사용량 계산
- 이전: Dashboard aiUsageCount가 Enterprise tier에선 항상 0 표시 → 관리자 모니터링 불가
- 수정 후: quota check는 여전히 skip (무제한이므로), 표시/모니터링용 usage는 정확

### MINOR-2: useAiFeatures deps 안정화 (eslint-disable 제거)
- src/hooks/useAiFeature.ts:180-234
- 기존: `[featureKeys.join(',')]` + `eslint-disable-next-line react-hooks/exhaustive-deps`
- 수정: `featureKeys.slice().sort().join('|')` 파생 stable key + `eslint-disable` 주석 삭제
- 효과: 순서만 다른 동일 집합은 재실행 안 됨 / featureKeys 참조 매번 새로 와도 effect 안정

### trial-* / sync-loops: projects.created_by → projects.owner_id
- 실제 `projects` 테이블 컬럼은 `owner_id` (20260403_multitenant_isolation.sql, delete-account, aiUsage 등 다수 증거)
- 수정 3개 파일 (각 파일의 getUserStats 내부 `.eq('created_by', userId)` 1회 + 헤더 주석):
  - supabase/functions/trial-expire-handler/index.ts:36
  - supabase/functions/trial-ending-reminder/index.ts:36
  - supabase/functions/sync-loops-contacts/index.ts:44
- 이전: 이 셋은 owner 프로젝트 조회가 silently 빈 배열 반환 → testCaseCount/testRunCount/teamMemberCount 전부 0으로 Loops에 전송됨
- 수정 후: 정상적인 집계 값으로 Loops 이메일 템플릿 merge tag 채워짐
- 부수: useAiFeature.ts 주석 "projects.created_by 기준" 2곳도 owner_id로 동기화

## 어디까지 됐냐
- TypeScript 타입체크 PASS (`npx tsc --noEmit` 에러 0개)
- 풀 빌드 PASS (`npm run build` 6.74s)
- 커밋 18407cd claude 브랜치에 있음, origin/claude push 완료
- npm run lint 스크립트 미존재로 ESLint 미실행 — tsc + build 통과로 대체

## 스킵 항목 (별도 티켓 필요)
- MAJOR-1 (vitest 단위 테스트 인프라): 프로젝트 테스트 러너 미구성. 인프라 변경이 커 이번 범위 밖
- MAJOR-2 (bigint 타입): 현재 `Number(data ?? 0)` 변환으로 실동작 안전. 타입만 명확히 하면 되는데 영향도 낮아 스킵
- MINOR-1 (i18n ai.usage.* 키 8개): Dev Spec §10 "권장" 수준이고 실제 UI 텍스트 변화 없음. UI 개편 시 함께

## 발견 사항 (사용자 확인 필요)
- `fetch-github-issues/index.ts:97`, `fetch-jira-requirements/index.ts:134` 에서 `.from('projects').select('id, created_by')` 확인됨
- 실제 projects 테이블은 owner_id이므로 `projectData.created_by` 는 항상 undefined
  → `!memberCheck && projectData.created_by !== user.id` 조건이 non-member인 프로젝트 owner에게도 Access denied 유발
- 이번 지시에선 "확인 후 결정"이라 범위 밖으로 처리 (별도 지시 받으면 수정)

## 배포 절차 (CEO 수동)
1. Edge Functions 재배포 (CEO가 일괄 배포 결정 시점에):
   - `supabase functions deploy trial-expire-handler trial-ending-reminder sync-loops-contacts`
   - `supabase functions deploy generate-testcases risk-predictor milestone-risk-predictor plan-assistant` (_shared/ai-usage.ts 변경이 있으므로 의존 함수 전부)
2. DB 마이그레이션 변경 없음 (이번 커밋은 코드만 수정)
3. 배포 후 검증:
   - Enterprise tier 사용자의 Dashboard aiUsageCount가 0 아닌 실제 값으로 표시
   - trial-* Edge Function 실행 후 Loops 이벤트의 testCaseCount 등 값이 0이 아닌 실제 집계로 전송

---

2026-04-20 (exploratory SegmentedBar): Exploratory 세션 4분할 activity bar @developer 구현 완료

## 뭘 했냐

### 신규 파일 1개
- src/components/SegmentedBar.tsx
  - Props: segments[{count,className,label?}], width(80), height(6), className
  - total=0 → 빈 gray 트랙 (fill 없음)
  - total>0 → count 비율로 flex-basis 분할, 0-count 세그먼트는 숨김 (경계 깔끔)
  - 세그먼트 사이 1px 흰색 border-right (마지막 제외)
  - 트랙 rounded-full (양끝만 둥글게, 내부 segment는 overflow-hidden으로 접합)
  - role="progressbar" + aria-valuenow={total} + aria-label=
    "Session activity: 2 Note, 3 Bug, 1 Obs, 0 Step"
  - .progress-bar-wrap 래퍼로 기존 모바일 <768px 숨김 CSS 재활용
  - ProgressBar와 독립 (단일 fill용 ProgressBar는 유지)

### 수정 파일 2개
- src/pages/milestone-detail/OverviewTab.tsx
  - SessionActivityCounts 인터페이스 export
  - OverviewExtra에 sessionActivityMap: Map<string, SessionActivityCounts> 추가
  - loadOverviewExtra 시그니처 확장: (milestoneId, runIds, sessionIds, aggregatedRunIds?)
  - sessionIds 있을 때 supabase.session_logs.select('session_id,type').in('session_id',...) 쿼리 추가
  - log.type 매핑: note→note / failed→bug / blocked→obs / passed→step
  - useQuery queryKey에 sessionIdsKey 추가 (세션 변경 시 재집계)
  - sessionActivityMap을 ExecutionSections props로 전달
- src/pages/milestone-detail/ExecutionSections.tsx
  - Props에 sessionActivityMap?: Map<string, SessionActivityCounts> 추가
  - Exploratory 섹션 L262 `<ProgressBar value={0} tone="gray" showLabel={false} />` 제거
  - `<SegmentedBar segments=[indigo/red/amber/purple] />` 로 교체
    - Note: bg-indigo-500 (#6366F1)
    - Bug:  bg-red-500    (#DC2626)
    - Obs:  bg-amber-500  (#D97706)
    - Step: bg-purple-500 (#7C3AED)
  - 색상은 session-detail/page.tsx:980-985 entryTypeButtons와 일치

### 건드리지 않음 (scope 외)
- src/components/ProgressBar.tsx — 단일 fill용 유지
- Sub Milestones / Test Plans / Runs 섹션 — 여전히 ProgressBar
- session_logs realtime 갱신 — Out of Scope
- 다른 페이지의 progress bar — Out of Scope

## 어디까지 됐냐
- TypeScript 타입체크 PASS (`npx tsc --noEmit` 에러 0개)
- 풀 빌드 PASS (`npm run build` 6.71s)
- 커밋 메시지: `feat(ui): Exploratory session SegmentedBar (note/bug/obs/step 활동 비율)`

## 다음에 할 것
- 커밋 + origin/claude push (main 금지)
- CEO 수동 확인:
  - Milestone Detail → Overview → Exploratory 섹션에 log 개수 비율 segment bar 표시
  - 로그 0개 세션은 빈 gray 트랙
  - 모바일 <768px 에서 segment bar 숨김 (기존 ProgressBar와 동일)
  - 스크린리더로 "Session activity: N Note, N Bug, N Obs, N Step" 읽기

---

2026-04-20 (badge/progress consistency): Milestone Detail 5색 배지 + ProgressBar @developer 구현 완료

## 뭘 했냐 (dev-spec + design-spec-milestone-badge-progress-consistency.md)

### 신규 파일 3개
- src/lib/statusPill.ts
  - STATUS_TO_VARIANT 매핑 (gray/blue/green/red/amber 5 variant)
  - resolveStatus / getStatusVariant / getProgressTone / humanizeStatus 유틸
  - past_due/overdue → red, active/started/in_progress → blue, under_review → amber (Violet에서 교체)
  - AC-11: 100% 완료 시 Overdue여도 green 우선
- src/components/StatusPill.tsx
  - Design Spec §1 Tailwind 클래스 그대로: `inline-flex items-center whitespace-nowrap rounded-full px-2 py-0.5 text-xs font-semibold`
  - VARIANT_CLASSES: bg-{gray|blue|green|red|amber}-100 text-{...}-700
  - size prop 타입만 유지(future-proof), md 단일 경로 구현
- src/components/ProgressBar.tsx
  - Design Spec §2: width 80px 기본 / height 6px / track bg-gray-200 / fill bg-{tone}-500
  - tone: blue/green/red/gray (LABEL_TONE_CLASSES로 라벨 색도 tone별 분기)
  - clamp (NaN/undefined/음수→0, 100초과→100)
  - role="progressbar" + aria-valuenow/min/max
  - 래퍼에 .progress-bar-wrap 클래스 → 모바일 숨김 CSS 훅

### 수정 파일 4개
- src/pages/milestone-detail/ExecutionSections.tsx
  - Props에서 getSubBadge / getRunStatusStyle 제거
  - 4개 섹션(Sub/Plans/Runs/Exploratory) 전부 `<StatusPill>` + `<ProgressBar>` 사용
  - Sub Milestones: 기존 인라인 progress div → `<ProgressBar>` (tone 로직에 AC-11 반영)
  - Test Plans: `<ProgressBar value={0}>` 빈 트랙 추가 (실제 집계는 Out of Scope)
  - Runs: `<StatusPill status={run.status} />` + 기존 pct 유지 ProgressBar
  - Exploratory: `<StatusPill status={session.actualStatus ?? 'new'} />` + ProgressBar value=0 showLabel=false
- src/pages/milestone-detail/OverviewTab.tsx
  - Props 시그니처에서 getSubBadge / getRunStatusStyle 삭제
  - ExecutionSections 호출부 깔끔하게 정리
- src/pages/milestone-detail/page.tsx
  - getStatusBadgeStyle / getRunStatusStyle 함수 (L738-757) 삭제 + 주석 1줄로 대체
  - Header 배지를 `<StatusPill status={milestone.status} />` 로 교체 (msBadge.dot 제거)
  - import StatusPill 추가
  - OverviewTab 호출부에서 getSubBadge / getRunStatusStyle props 제거
- src/index.css
  - `.badge` 주석에 "NEW CODE: use <StatusPill />" 가이드 추가
  - @media (max-width: 767px) .mo-sec-card .row .progress-bar-wrap { display: none; } 추가 (Design Spec §6)

### 건드리지 않음 (scope 외, 후속 PR)
- src/components/StatusBadge.tsx — TC Run 결과 전용 (passed/failed/blocked/retest/untested dot 배지), 네이밍 충돌 방지 위해 분리 유지
- src/pages/project-milestones/* (MilestoneCard / AdhocPanel / AdhocRunCard / MilestonePlanList) — Dev Spec §4에 "후속 PR" 명시
- src/pages/plan-detail/page.tsx badge-warning 등 — 동일 후속 PR 범위

## 어디까지 됐냐
- TypeScript 타입체크 PASS (`npx tsc --noEmit` 에러 0개)
- 풀 빌드 PASS (`npm run build` 6.72s)
- npm run lint 스크립트 미존재 (기존 진행 기록과 동일) → tsc + build 통과로 대체
- AC 12개 모두 코드 수준 구현 완료 (AC-1~12)

## 다음에 할 것
- 수동 QA (Dev Spec §10 체크리스트 10항목):
  - Milestone Detail Overview 페이지에서 `active` plan 배지가 Blue로 (기존 Amber)
  - `under_review` run 배지가 Amber로 (기존 Violet)
  - `past_due` sub 배지가 Red로 (기존 Orange)
  - Test Plans / Exploratory 카드에 0% 빈 progress bar 표시
  - 모든 "In Progress" 배지의 background가 동일한가
- 커밋 메시지: `feat(ui): Milestone detail 카드 배지 5색 통일 + ProgressBar 적용`
- 후속 PR 범위 (out of scope, 별도 티켓):
  - Test Plans 실제 progress 집계 쿼리 (loadOverviewExtra 확장)
  - Exploratory Session progress 로직 (done/total 스키마 결정 선행 필요)
  - project-milestones / plan-detail의 레거시 badge-* 클래스 마이그레이션
  - i18n 키 추가 (현재 label은 StatusPill 내부 영문 하드코딩, 기존 동작 동일)
