2026-04-19: Milestone Overview Redesign + Issues 탭 메타데이터 (Option B)

## 뭘 했냐
- Milestone Detail 탭 5개(Results/Status/Activity/Burndown/Issues) → 3개(Overview/Activity/Issues)로 재편
- Overview 탭 신규 구현: Burndown(Recharts) + 4 KPI strip + Intel 6-card grid + Execution sections + Contributors Top 5
- Intel 6 카드: Failed&Blocked (span 2) / Velocity 7d / Top-Fail Tags (신규) / ETA vs Target / AI Risk Insight (span 2) / Activity 24h (span 2)
- Execution Sections: Sub Milestones / Test Plans (신규) / Runs (Planned vs Direct 뱃지 구분) / Exploratory
- `?tab=results|status|burndown` → `?tab=overview` replace:true 리다이렉트 (AC-C2)
- MilestonePlanList 헤더에 "Open detailed view →" 버튼 추가 (Edit 왼쪽)
- Plan Detail / Milestone Detail Issues 탭이 공통 <IssuesList /> 컴포넌트 사용 (priority/status/assignee 메타데이터 표시)
- 이슈 메타데이터 Option B DB 저장:
  - 신규 `test_results.jira_issues_meta` JSONB 컬럼 + 기존 `github_issues` 필드 확장
  - `create-jira-issue` 확장: 생성 후 REST API로 priority/status/assignee fetch → JSONB append
  - `create-github-issue` 확장: 응답 파싱 → JSONB append
  - `sync-jira-metadata` 신규 (sync-jira-status 폐기, 스텁은 deprecated 주석)
  - `sync-github-metadata` 신규 (지수백오프 + rate limit 처리)
  - `github_sync_log` 테이블 + RLS 신설
- i18n: `milestones.detail.overview.*` 네임스페이스 / `common.issues.*` 네임스페이스 추가 (en/ko)
- CSS: src/index.css 맨 아래 `.mo-*` 블록 전체 추가 (250+줄)

## 어디까지 됐냐
- 마이그레이션: supabase/migrations/20260419_issue_metadata.sql 작성 (배포 필요)
- Edge Functions: 4개 작성/수정 (배포 필요)
  - create-jira-issue (확장)
  - create-github-issue (확장)
  - sync-jira-metadata (신규)
  - sync-github-metadata (신규)
- 프론트 코드: tsc --noEmit 통과, vite build 성공
- Pg_cron 스케줄 등록: Supabase Dashboard에서 수동 등록 필요 (migration 파일 주석 참조)

## 다음에 할 것
- supabase db push (20260419_issue_metadata.sql 배포)
- supabase functions deploy (sync-jira-metadata, sync-github-metadata, create-jira-issue, create-github-issue)
- Supabase Dashboard에서 pg_cron 등록 (6시간 주기, 위 migration 파일 주석 참조)
- create-jira-issue/create-github-issue 호출 부분 프론트에서 `test_result_id`/`run_id`/`project_id` 전달 (optional — 현재 없으면 메타데이터는 다음 cron 주기에 채워짐)
- 기존 `sync-jira-status` 폴더는 배포 후 삭제 고려 (CLAUDE.md: 허락 없이 삭제 금지)
- dev-spec AC-A5 지수백오프: jira_sync_log에 failed keys 기록 부분은 기본 로직만 구현 (상세 로그 구조는 필요 시 보강)
- Overview: mockup_3의 "Behind ideal" KPI 대신 ETA 카드로 분리한 디자인 명세 차이는 디자이너 의도대로 반영
